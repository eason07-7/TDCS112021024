# Final Presentation 實驗記錄

> 此檔**只記錄** `final_presentation/` 資料夾內的工作。外部 `TDCSprecentater/實際紀錄.md`、`ai_workspace/`、`112021134/` 屬於另一個專題，**不受本紀錄約束**。

---

## ⭐ PPT 製作規範（最高優先；所有流程以產出此 PPT 為最終目標）

**必須包含的頁面與內容**：

1. **封面** — 交流道名稱 + 時間區間 + 小組成員
   - 小組成員欄位：學號、姓名、貢獻度百分比（5% ~ 5%）、負責內容（說明）、YouTube URL（分享）
   - *組員資訊全部留空供使用者自行填寫*

2. **動機與目的**

3. **TDCS 資料上傳 AWS S3**
   - 時程標註：1 週 (7) / 2 週 (8) / 3 週 (9) / 1 個月 (10)
   - *使用者另備三張截圖，此頁留空間供放圖*

4. **觀察 XX 交流道 4 匝道總車流**
   - 欄位：GantryID_O 或 GantryID_D；北上(N) / 南下(S)
   - 交流道別：
     - `01F2930N` 下營系統 → 新營
     - `01F2930S` 新營 → 下營系統
     - `01F3019N` 麻豆 → 下營系統
     - `01F3019S` 下營系統 → 麻豆
   - 內容：資料視覺化 + 文字解釋

5. **不同車種 VS. 不同星期**
   - 車種：VT=31 / 32 / 41 / 42 / 5
   - 內容：資料視覺化 + 文字解釋

6. **方法改進**
   - 資料下載？處理方式？程式自動化？
   - 參考 `D:\p\112021134\DOWNLOAD_WORKFLOW.md`（兩腳本工作流）

7. **資料正確性稽核方式**
   - 例：第三者人力？程式自動檢查？
   - 本專題採用：Gemini API 跨檔案自動分析（statistical z-score + LLM 輔助）

8. **小組會議討論紀錄（至少兩次）**
   - 欄位：時間、地點、討論事項、檢討、（照片）
   - *內容與照片留空*

9. **學習心得**
   - 每位組員都要寫
   - *內容留空*

**備註**：「不知道的東西可以填空」—— 組員資訊、S3 上傳截圖、會議紀錄、學習心得皆為留白項。腳本與 AI 分析產出的數據部分要具體、精準。

---

## 專題資訊

- **題目**：國道1號 下營系統-麻豆段 3月流量分析（期末報告）
- **時間區間**：2026/03/01 ~ 2026/03/31
- **目標匝道**：
  - 01F2930N（北上：下營系統→新營）
  - 01F2930S（南下：新營→下營系統）
  - 01F3019N（北上：麻豆→下營系統）
  - 01F3019S（南下：下營系統→麻豆）

---

## [2026-04-14] 工作區建立

### 結構
```
final_presentation/
├── step0_s3_download/   (S3 下載)
├── step1_cleaning/       (清洗)
├── step2_visualization/  (兩類圖 × 4 匝道 + 總覽)
├── step3_ai_analysis/    (Gemini 2.5 Flash 跨檔案分析)
├── step4_ppt/            (PPT 內容主檔)
└── 實驗記錄.md (本檔)
```

### 腳本來源追溯（唯讀引用外部檔，自身在 final_presentation 內修改）

| 新檔 | 來源 | 改動 |
|---|---|---|
| step0/download_from_s3.py | 自寫（參考 `upload_only_2025.py` 反向操作） | 新增 |
| step1/clean_202603.py | `TDCSprecentater/TDCS_M06A_Batch_Cleaning_202603.py` | GantryID 改 4 匝道；新增 O/D 雙向命中；新增 `TargetGantry` 欄位 |
| step2/viz_4gantries.py | `TDCSprecentater/TDCS_M06A_Batch_Processing_05F0287N.py` | 4 匝道迴圈；兩類圖（總車流 24H + 車種×星期） |
| step3/analyze_all.py | `ai_workspace/src/tkinter_main.py` 的 `_call_gemini_report` | 去除 Tkinter GUI；改讀清洗後 CSV 全量 + 圖表清單；加本地備用 |
| step3/rag_knowledge.json | `ai_workspace/rag/rag_route_knowledge_full.json` | 雪山隧道 → 下營/麻豆路段 |

---

## [2026-04-14 02:30~02:50] 事件紀錄：Bucket 誤判與環境變數小寫問題

### 事件 A — boto3 找不到 credentials
- **現象**：直接 `python -c "import boto3; s3.list_objects_v2(...)"` 報 `NoCredentialsError`。
- **原因**：Python 子行程沒繼承 `.env`，需要先顯式載入。
- **處理**：在 `download_from_s3.py` 中自寫 `load_dotenv_file()`（仿 `upload_only_2025.py`）於 import boto3 前 setdefault 環境變數。

### 事件 B — `aws_session_token` 小寫無效
- **現象**：即使 `.env` 有 session token，boto3 仍然拒絕臨時憑證（Learner Lab 是 assumed-role，必須帶 token）。
- **原因**：`D:\p\112021134\.env` 第 4 行寫成小寫 `aws_session_token=...`，第 5 行大寫 `AWS_SESSION_TOKEN=`（空值）；boto3 只認大寫。`setdefault` 讀到小寫後沒補大寫版本，導致 boto3 拿到空 token。
- **處理**：`download_from_s3.py` 在 `load_dotenv_file` 之後補一段：
  ```python
  if os.environ.get("aws_session_token") and not os.environ.get("AWS_SESSION_TOKEN"):
      os.environ["AWS_SESSION_TOKEN"] = os.environ["aws_session_token"]
  ```
- **驗證**：`sts.get_caller_identity()` 成功回傳 `assumed-role/voclabs/user3852248=112021134@live.asia.edu.tw`。

### 事件 C — Bucket 名稱誤判 124 vs 134
- **現象**：`list_objects_v2(Bucket=<.env值>, Prefix='202603/')` 回傳 `KeyCount=0`，以為 S3 是空的。
- **除錯**：改用 `list_buckets()` 看到帳號下其實有 **兩個** bucket：
  - `112021124trafficdatacollectionsyste`（空）
  - `112021134trafficdatacollectionsyste`（有 202603/ 週分子夾）
- **根因**：copy 到 `step0_s3_download/.env` 的那份 S3_BUCKET 寫成 `112021124`（與使用者學號 `112021134` 只差一碼，疑似手動 typo 或舊版 .env）。
- **處理**：把 `final_presentation/step0_s3_download/.env` 第 9 行改為 `S3_BUCKET=112021134trafficdatacollectionsyste`。
- **驗證**：`list_objects_v2(Prefix='202603/', Delimiter='/')` 列出 5 個週分子夾。

### 事件 D — 背景任務終端輸出看不到
- **現象**：`python download_from_s3.py 2>&1 | grep -v ... | tail -80` 在執行期間輸出檔空白。
- **原因**：`tail` 要等 stdin EOF 才 flush，`grep` 管線也有緩衝，下載尚未結束就看不到任何 print。
- **替代方案**：改以磁碟計數（`ls ... | wc -l` + `du -sh`）作為進度指標；未來若要即時 log，改寫成 `python script.py > log.txt 2>&1` 然後用 `tail -F log.txt` 或 `Monitor` 工具。

---

## [2026-04-14 02:51] Step 0 S3 下載啟動

### AWS 狀態
- Identity：`arn:aws:sts::401322580935:assumed-role/voclabs/user3852248=112021134@live.asia.edu.tw`
- Region：`us-east-1`
- Bucket：`112021134trafficdatacollectionsyste`（經事件 C 修正）

### S3 目錄結構
```
s3://112021134trafficdatacollectionsyste/202603/
  ├── 20260301~20260307/   (第 1 週)
  ├── 20260308~20260314/   (第 2 週)
  ├── 20260315~20260321/   (第 3 週)
  ├── 20260322~20260328/   (第 4 週)
  └── 20260329~20260331/   (月底)
```

### 下載設定
- 輸出：`step0_s3_download/raw_202603/`（扁平化，不保留週分子夾）
- 已存在且大小一致則跳過（`SKIP_IF_EXISTS=1`）
- 預期檔數：744（31 天 × 24 小時）
- 背景任務 ID：`bgedra6p4`

### 進度採樣
| 時間 | 已下載 | 容量 |
|---|---|---|
| T+90s | 33 / 744 (4%) | 0.87 GB |
| T+5min | 78 / 744 (10%) | 2.1 GB |
| T+12min | 405 / 744 (54%) | 12 GB |

### 備註
- `.env` 中 `aws_session_token` 為小寫，boto3 只吃大寫，已在 `download_from_s3.py` 自動補 uppercase。
- 終端輸出被 `| grep -v | tail` 管線緩衝，下載期間看不到即時訊息，以磁碟計數為準。

---

## [2026-04-14 04:25] 事件 E — S3 下載後段 AccessDenied / Session 疑似過期

### 現象
下載到第 517 檔（剛進入 3/22 13:00）時崩潰：
```
[error] S3 ClientError code=AccessDenied
  ... is not authorized to perform: s3:GetObject
  resource: .../20260322~20260328/20260322/..._20260322_130000.csv
  with an explicit deny in an identity-based policy
```

### 診斷
對 3/22、3/23、3/31 各抽一檔呼叫 `head_object`，全部 403 Forbidden。
但 `list_objects_v2` 仍可用 → 權限殘留但 GetObject 被拒。
推測為 AWS Learner Lab Session Token 已過 4 小時壽命上限，只保留唯讀列表權限。

### 實際拿到的資料
| 日期範圍 | 狀態 | 檔數 |
|---|---|---|
| 3/1 ~ 3/21 | 完整 | 21 × 24 = 504 檔 |
| 3/22 | 部份 | 13 / 24（0~12 時） |
| 3/23 ~ 3/31 | 未下載 | 0 檔 |

### 決策
**不等 Lab 重啟、直接用 3/1~3/21 共 21 天（完整 3 週）進行清洗與分析**。理由：
1. 21 天 × 4 匝道 × 24H × 5 車種 已足夠產出有統計意義的發現（週間規律、車種分布、尖峰時段皆可涵蓋）。
2. PPT 規範裡 TDCS 上傳進度是獨立的截圖（使用者自備），跟本地資料量沒綁定。
3. 3/22 半天資料會造成日均偏差，**在清洗中明確排除 day=22**。
4. 若 Lab 重啟後可補抓，屆時只要重跑 `download_from_s3.py`（`SKIP_IF_EXISTS=1` 會自動補缺）並重跑 Step 1~3。

### 清洗腳本調整
`step1_cleaning/clean_202603.py` 的 `DATE_END` 原為 31；為避開 3/22 半日污染，此次 run 改為 21（含）。下次補資料成功後可調回 31。

---

## [2026-04-14 09:00] Lab 重啟後恢復下載
- 事件 E 的 Session 過期已於 Lab 重啟後更新。
- 新憑證 ACCESS_KEY：`ASIAV24FQTPDWZUKOVWB`（舊：`...2GBOHJIC`）
- 驗證：對 `3/31 23:00` 檔 `head_object` 回 `size=10106327`（舊憑證 403）。
- 已續跑 `download_from_s3.py`（背景任務 `buu2gup05`），預計補抓 3/22 後半 + 3/23~3/31 共約 227 檔。
- `DATE_END` 將於下載完成後改回 31。

---

## [2026-04-14 08:42] Step 0 下載完成（含補檔）
- 最終檔數：**741** 檔 / 22 GB
- 每日檔數：3/1~3/25、3/27~3/31 皆完整 24 檔；**3/26 只有 21 檔**（見事件 F）
- `DATE_END` 已復原為 31（全月涵蓋）。

---

## [2026-04-14 08:42] 事件 F — 3/26 來源缺 3 小時檔

### 現象
下載完成後逐日點檔數，所有日子都是 24 小時、**唯獨 3/26 只有 21 小時**。

### 診斷
直接對 S3 `list_objects_v2` 查 `202603/20260322~20260328/20260326/` prefix，結果：
```
3/26 hours in S3: ['00', '01', '02', '03', '04', '05', '06', '07', '08', '09',
                   '10', '11', '12', '13', '14', '18', '19', '20', '21', '22', '23']
```
- **缺 15:00、16:00、17:00**
- **S3 來源本身就沒有這 3 個檔**，非本地下載錯誤（`skipped=517, downloaded=224, total_seen=737`，數字吻合）。

### 可能原因
- 原始 TDCS M06A 每小時 tar.gz 偶發缺檔（參考 `download_only_2025.py` 的 hourly fallback 機制）。
- 3/26 是星期四下午時段（15~17 時），疑為 TDCS 伺服器當天該時段上傳中斷。

### 影響評估
- 3/26 日均車流會比實際低約 12.5%（3/24 小時）。
- 若以「小時平均」看則無偏差；「日均」時需加註此日為不完整樣本。

### 處理
1. **Step 1 清洗**：不特別排除 3/26，保留其 21 小時資料（避免損失 1 天星期四樣本）。
2. **Step 2 視覺化**：24H 疊加圖會自動顯示 3/26 在 15~17 時的 0 值，視覺上可識別為異常。
3. **Step 3 AI 分析**：在統計摘要中註記「3/26 僅 21 小時」，讓 LLM 在「資料正確性稽核」段落主動指出。
4. **PPT 說明**：方法改進或資料稽核頁可援引此例，說明自動化流程如何偵測源頭缺檔。

---

## [2026-04-14 08:43] Step 1 清洗執行中
（下載完成後自動執行並回填）

## [待填] Step 2 視覺化結果
（清洗完成後自動執行並回填）

## [待填] Step 3 AI 分析結果
（視覺化完成後自動執行並回填）

## [待填] Step 4 PPT content.md 更新
（AI 分析完成後自動填入摘錄）
