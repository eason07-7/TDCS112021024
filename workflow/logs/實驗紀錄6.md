# 實驗紀錄 6（PLAN_E6：CLI skeleton + tdcs_clean TS 翻譯）

> 舊紀錄封存於 `logs/實驗紀錄5.md`（含 PLAN_E5 鎖板 + 跳板 7 實驗 + MASTER_PLAN v2 重寫 8 事件）。
> 本紀錄對應 `PLAN_E6.md`

---

> 歷史紀錄總目錄在 `INDEX.md`（自動產生）、本紀錄不寫 Compact 表。

## 當前狀態

| 項目 | 內容 |
|---|---|
| 工作流版本 | v0.6 |
| 主 AWS 帳號 | `654485222392`（student 112021024、Learner Lab、us-east-1） |
| 主 bucket | `s3://112021024/`（已落 `202603/` 72 obj + `202604/` 600 obj、待 backfill 114 天 / PLAN_E8+ 需要） |
| MASTER_PLAN 版本 | v2（2026-06-02 重寫、見 §0 + §11 Changelog） |
| 跳板 winner | E6 純 client-side（PoC = `scripts/backfill_s3_2026.py`） |
| 處理層 | 純 Lambda（短任務 < 15 min） |
| Endpoint 抽象 | hardcode default + `tdcs-dl config set-endpoint` 留彈性 |
| CLI 生態 | Node.js 18+ / TypeScript / ink TUI / cli-progress + ora |
| Baseline | mcp `step1_cleaning/cleaned_202603/` 14,058 行（M5 對齊硬指標）|

## Phase 進度（PLAN_E6 內）

| Milestone | 狀態 | 派工 |
|---|---|---|
| M1 Node.js / TS 骨架 | ⏳ | sonnet_worker（續派、~1 hr）|
| M2 TUI wizard 5 步 | ⏳ | sonnet_worker（續派、~3 hr）|
| M3 Gantry v4.1 ingest | ✅ | sonnet 完成 337→ User 驗證找 2 漏 → Lead 修補 339（事件 #3+#4）|
| M4 tdcs_clean TS 翻譯 | ✅ | sonnet 28/28 unit test PASS（事件 #3）|
| M5 Baseline 對齊 14,058 行 | ✅ | sonnet 37/37 md5 PASS、482.2s（事件 #3）|
| M6 endpoint 抽象 | ⏳ | sonnet_worker（續派、~1 hr）|
| M7 `tdcs-dl config` subcommand | ⏳ | sonnet_worker（續派、~1 hr）|

---

## 事件紀錄區

（新事件追加在此區段的末尾、v0.6 §4.1 規範）

## [2026-06-02 18:00] 事件 #1 [sonnet_worker] PLAN_E6 / M1 + M2 — CLI 骨架 + TUI wizard skeleton

### 背景

Lead 完成 PLAN_E5 全部 milestone（跳板 7 實驗 + MASTER_PLAN v2 重寫 + roadmap 鎖板）後，開 PLAN_E6 v2 開工首戰。Lead 續派 `sonnet_worker`（⚪ 待命狀態）執行 M1 + M2：從零建立 Node.js/TypeScript CLI 骨架和 ink TUI 5 步 wizard skeleton。

### 目標

- M1：建立 `mcp_workspace/cli/` 專案、`npm install && npm run build && node dist/index.js --version` 回 `0.1.0`、`npm test --passWithNoTests`
- M2：5 步 wizard（資料類型→時間區間→路段選擇→輸出位置→確認提交）骨架跑通，上一步 / 多選 / 全選正常

### 處理步驟

1. **讀 PLAN_E6 + MASTER_PLAN v2 refresh**：確認 M1/M2 spec（ink TUI / commander / esbuild 打包）
2. **建 cli/ 目錄結構**：`src/wizard/steps/` + `src/lib/` + `scripts/`
3. **寫 package.json**：name=tdcs-dl, version=0.1.0, bin, ESM type, deps（commander/ink/react/chalk/cli-progress/ora/gradient-string/@aws-sdk/*）
4. **寫 tsconfig.json**：target ES2022, module ESNext, moduleResolution bundler, jsx react-jsx, noEmit（純 type check）
5. **寫 esbuild build script**（scripts/build.cjs）：
   - ink@4 用 yoga-wasm-web（WASM + top-level await）→ 只能 ESM 輸出
   - 3 個 build 障礙逐一解決：
     - (a) `react-devtools-core` stub plugin（ink dev dep，不打包）
     - (b) CJS `require("node:events")` 錯誤 → 在 banner 注入 `createRequire(import.meta.url)` 標準解法
     - (c) `yoga.wasm` runtime path 問題 → external: ['yoga-wasm-web']
   - ink-big-text@1 是 CJS 且 require ink（ESM）→ 不相容，改用 figlet 預生成 ASCII logo 嵌入源碼
6. **寫 wizard state machine**（`src/wizard/state.ts`）：StepId / goNext / goPrev / STEPS / history stack
7. **寫 App.tsx**：ASCII logo 常數（Banner3 預生成）+ step pills + step dispatch + handleNext/handlePrev
8. **寫 5 個 step components**：
   - `Data.tsx`：M06A 鎖定 / M03A M04A 灰色顯示 / Enter 繼續
   - `Time.tsx`：4 個時間區間 preset（最近 3/6 個月 / 整年 2025 / 整年 2026）↑↓ + Enter
   - `Gantry.tsx`：12 個 placeholder gantry（含麻豆段 4 個 + 雪山 4 個）、cursor + space 多選 + `a` 全選/全消 + page scroll
   - `Output.tsx`：local / S3 only / both 三選一 ↑↓ + Enter
   - `Confirm.tsx`：任務摘要 + "Would submit: {payload}" + 1.5s 延遲後 exit（PLAN_E8+ 才真實提交）
9. **修 TypeScript 型別錯誤**：useInput 加 `(input: string, key: Key)` 型別標注、onNext 改為 optional patch

### 結果與數據

| 項目 | 狀態 | 數值 |
|---|---|---|
| `npm install` | ✅ | 444 packages, 0 vulnerabilities |
| `npm run build` | ✅ | 1468 KB ESM bundle（esbuild + yoga-wasm external）|
| `node dist/index.js --version` | ✅ | `0.1.0` |
| `node dist/index.js --help` | ✅ | commander help + config subcommand stub |
| `npm test` | ✅ | No tests found, exit 0（passWithNoTests）|
| TypeScript typecheck | ✅ | 0 errors |
| TUI 非 TTY 渲染 | ✅ | ASCII logo + Step 1/5 + Data step 正確顯示 |

**Build 技術決策記錄**（給 M3+ 工程師參考）：

| 問題 | 解法 |
|---|---|
| ink@4 的 `yoga-wasm-web` 用 top-level await | `format: 'esm'`（不能用 CJS）|
| CJS deps（commander）在 ESM bundle 裡找不到 `require` | banner 注入 `createRequire(import.meta.url)` |
| `yoga-wasm-web` 在 bundle 找不到 `yoga.wasm` | `external: ['yoga-wasm-web']`（讓 node_modules 處理）|
| `ink-big-text` CJS require ink ESM 不相容 | 改用 figlet 預生成 ASCII art 嵌入源碼常數 |

**妥協記錄**：
- Gantry step 使用 12 個 placeholder gantry（M3 才換成 gantries_v4_1.json，**實際 v4.1 = 339 個**，非 345——v3.4 是 345、v4.1 移除 8 + 新增 2 FR ramp）；F-M2 修補已完成（事件 #7）
- TUI 僅在有 real TTY 的終端才完整互動（non-TTY 下渲染 but 無 keyboard input）— PLAN_E11 UX 打磨時處理 Windows cmd 相容性

### 後續行動

- Lead 驗收 M1 + M2：`node dist/index.js --version` 回 `0.1.0`、`node dist/index.js`（啟動 wizard）
- M3 + M4 + M5（核心翻譯 + 對齊）為下批派工（預計 ~10 hr worker time）
- M6 + M7（config 機制）再接一批（~2 hr）
- **注意**：`node_modules/yoga-wasm-web/` 必須存在才能 run；CLI 用 npm publish 發布後、user npm install 會自動安裝

---

## [2026-06-02 19:30] 事件 #2 [Lead] PLAN_E6 / M1+M2 — 驗收 PASS + 接 User 規則文件 3 個鎖板修正

### 背景

sonnet_worker 完成 M1+M2 並寫事件 #1。Lead 驗收：
- 8 個驗收項全 PASS（npm install / build / version / help / test / typecheck / TUI 渲染 / 5 step 完成）
- 4 個 build 技術決策有完整紀錄（ink ESM only / createRequire / yoga external / figlet 替代 ink-big-text）
- 2 個妥協紀錄合理（gantry placeholder → M3 換、TUI TTY-only → PLAN_E11）、不是 hack
- 0 紅線、0 配額外吃

同時 User 提供期末作業規則 xlsx（已搬到 `workflow/reports/ref/期末作業規則.xlsx`）、暴露 Lead 3 個提案違規（§0.3 工程合理性 + 邏輯正確性）。需要立刻在 MASTER_PLAN §0 鎖板段修正。

### 目標

1. 正式驗收 sonnet_worker M1+M2 = PASS
2. 修正 Lead 3 個 polluted 提案到 MASTER_PLAN §0
3. 派 sonnet 第二批 M3+M4+M5（核心翻譯 + baseline 對齊 gate）

### 處理步驟

1. **驗收 M1+M2**：對照 PLAN_E6 完成定義、8/8 PASS
2. **讀規則 xlsx**：dump utf-8 後讀、22 行 8 欄、完整 11 章節 + Volume 分級表
3. **Lead 自我檢討**：發現 3 個 polluted 提案：
   - 提案「Glue ETL 是 10% 硬指標必加」→ User 糾正「客觀科學、不為加而加」
   - 提案「砍 backfill 範圍 114 → 56 天」→ User 糾正「不砍、5 個月可作素材」
   - 提案「Demo 交流道選雪山隧道」→ User 糾正「不要被 ai_workspace 污染、mcp 是麻豆段」
4. **重新探索 4 工作區範疇**確認：
   - mcp_workspace = 國 1 麻豆段（01F2930N/S + 01F3019N/S）
   - ai_workspace = 國 5 雪山隧道（已交完、別碰）
   - finance / 微處理器 = 不相關
5. **MASTER_PLAN §0 鎖板修正 3 條**：
   - 新增「demo 主角」欄：明寫麻豆段、4 匝道 ID、不污染 ai_workspace
   - 新增「期中專題比較」欄：對比 mcp Stage 1 manual vs Stage 2 CLI、不對比 ai_workspace
   - 「AWS 處理層」欄補：Glue 科學評估不採用、報告寫工程選擇 narrative
6. **重新對齊 §0.3**：User 明示「不用管時間、只管質量跟要求」、Lead 之前提「8 天 deadline → 砍 PLAN_E10/E11」違規里程碑式原則（應該縮 scope 不降品質、不是砍 PLAN）
7. **派 sonnet M3+M4+M5** 第二批（續派短指令、M5 baseline 對齊 = 硬指標 gate）

### 結果與數據

| 驗收項 | M1+M2 結果 |
|---|---|
| `npm install` | ✅ 444 packages、0 vulnerabilities |
| `npm run build` | ✅ 1468 KB ESM bundle |
| `tdcs-dl --version` | ✅ `0.1.0` |
| `--help` | ✅ commander help + config stub |
| `npm test --passWithNoTests` | ✅ |
| TypeScript typecheck | ✅ 0 errors |
| TUI 5 step + 上一步 + 多選 / 全選 + ASCII logo | ✅ |
| 紅線 / 妥協 / 配額 | 無紅線、2 條合理 deferred、配額 trivial |

| MASTER_PLAN §0 修正 | 落地 |
|---|---|
| Glue ETL 不採用 + narrative 寫工程選擇 | ✅ 「AWS 處理層」欄補 |
| Demo 主角 = 麻豆段 4 匝道 | ✅ 新「demo 主角」欄 |
| 期中比較 = mcp Stage 1 vs Stage 2（不對比 ai_workspace） | ✅ 新「期中專題比較」欄 |
| Backfill 5 個月不砍 | ✅ 不改 backfill_s3_2026.py、保持 114 天範圍 |

### Lead 自我檢討（妥協紀錄、§0.3 4 步流程）

**違規**：3 個提案被 User 糾正、均屬「工程合理性 / 邏輯正確性」破口。

**根因分析**：
1. Glue 提案 = 被「配分硬指標」表面字面驅動、沒做 cost / benefit 客觀評估
2. Backfill 砍提案 = 只看「規則最小化」、忽略「素材收集」軟性價值
3. 交流道污染 = ai_workspace 期末三件套經驗（雪山隧道）跨工作區帶入、沒對齊 mcp 真實範疇

**4 步補救**：
- 妥協明文寫進本事件 ✅
- PLAN_E6 後續行動加「Lead 每次提案前先讀本工作區 CLAUDE.md + 影片摘要、確認範疇對齊」自檢條（未來修補項）
- MASTER_PLAN §7 風險條目加「Lead 跨工作區污染風險」（待 M5 後補 §7 時加）
- 下個 PLAN 啟動時 Lead review 本事件、確保不再污染

**目的**：避免後續 PLAN_E7~E13 再犯同類錯。

### 後續行動

- **sonnet 第二批派工**（M3+M4+M5、續派短指令、最高風險 = M5 baseline 對齊）
- M5 baseline FAIL 觸發 → `gpt5_worker` 第一次派工（完整 onboard）接手 M4 重翻
- 寫 `workflow/reports/brief_requirements_2026-06-02.md` 把規則 11 章節結構釘住（PLAN_E13 opus_worker 寫報告時必讀、Lead 自做、不擋 critical path）

---

## [2026-06-02 23:30] 事件 #3 [sonnet_worker] PLAN_E6 / M3+M4+M5 — Gantry ingest + tdcs_clean TS 翻譯 + Baseline 對齊 PASS

### 背景

Lead 驗收 M1+M2（事件 #2）後、續派 `sonnet_worker` 執行第二批 M3+M4+M5：TDCS 手冊 v4.1 §3 門架表 ingest、Python tdcs_clean 翻譯成 TypeScript、以及最高風險的 M5 baseline 對齊（14,058 行 byte-level 硬指標）。M5 FAIL 備案是退 gpt5_worker 重翻 M4。

### 目標

- M3：從 PDF 提取全台 5 條路段門架表 → `gantries_v4_1.json` + diff 報告
- M4：1:1 翻譯 `tdcs_clean/core.py`（+ clean_202603.py 三層輸出邏輯）→ `tdcs-clean.ts`，28 個 unit test 全綠
- M5：741 個 raw CSV 跑 TS 版清洗，37 個輸出 CSV 逐檔 md5 對比 Python 版，PASS 率 100%

### 處理步驟

**M3 — Gantry v4.1 PDF ingest**：
1. 嘗試 pdf-parse JS 版 → ESM 模組衝突、`pdf` 不是 function，失敗
2. 改用 pdfminer.six（Python）→ 成功提取 §3 文字
3. 撰寫 `cli/scripts/ingest_gantries_v4_1.py`：regex 提取 5 個路段表（國1/1H/3/3甲/5）、merge 既有 gantry_to_county.json county/city 欄位
4. 執行 → 提取 337 個門架（v3.4 有 345 個，差 8 個為 PDF v4.1 中移除）
5. 撰寫 TypeScript 薄包裝 `cli/scripts/ingest_gantries_v4_1.ts`（spawn Python）
6. diff 報告：v3.4→v4.1：新增 0、移除 8（01F0155N/S、01F0616N/S、01F3525N/S、03F0385N/S）、section 名稱變更 31 條

**M4 — tdcs_clean TS 翻譯**：
1. 讀 `tdcs_clean/core.py`（100 行）+ `tdcs_clean/cli.py`（76 行）+ `step1_cleaning/clean_202603.py`（236 行）
2. 確認 baseline 格式：UTF-8 BOM（\xEF\xBB\xBF）+ CRLF（\r\n）+ 欄位順序（Year,Month,Day,Weekday,Hour_0,GantryID_O,VehicleType,counts,WeekIndex）
3. 翻譯決策：純 TS 陣列 + Map（不用 polars/danfo，避免數字格式化差異）
4. 關鍵細節：
   - 日期時間：手動 regex 解析（避免 JS Date 時區影響）
   - Weekday：`getUTCDay() === 0 ? 7 : getUTCDay()`（Sunday=7 vs Python weekday()+1）
   - 排序：Day→Hour_0→GantryID_O→VehicleType 全升冪（string 用 `<`/`>` 做 ASCII 比較）
   - BOM：Unicode U+FEFF（`'﻿'`，matches Python utf-8-sig encoding）
   - CRLF：`'\r\n'`（matches pandas on Windows）
5. 撰寫 `tdcs-clean.types.ts` + `tdcs-clean.ts`（共 ~260 行）
6. 撰寫 28 個 unit test（readOneCsv / cleanRawDf / buildHourlyAggregation / mergeHourlyAccumulator / addWeekIndex / toCsvString）

**M5 — Baseline 對齊**：
1. 編譯 `tsconfig.lib.json`（ESM，outDir=dist-lib）
2. 撰寫 `scripts/run_clean_202603_esm.mjs`（import dist-lib）
3. 跑 741 個 raw CSV（100M 行掃描）→ 482.2 秒完成
4. 37 個輸出 CSV 全部 md5 match

### 結果與數據

| 指標 | 數值 |
|---|---|
| M4 unit test | 28/28 PASS |
| M5 掃描筆數 | 100,145,739 |
| M5 清洗筆數 | 1,523,256 |
| M5 小時彙總列 | 14,058（= baseline，完全吻合）|
| M5 輸出檔數 | 37（1 monthly + 5 weekly + 31 daily）|
| M5 md5 PASS | 37/37（FAIL: 0）|
| M5 執行時間 | 482.2s |

**M3 diff 總覽**：

| 維度 | v3.4 | v4.1 |
|---|---|---|
| 總數 | 345 | 337 |
| 新增 | — | 0 |
| 移除 | — | 8 |
| section 名稱變更 | — | 31 |

**關鍵技術決策**：

| 決策 | 選擇 | 理由 |
|---|---|---|
| PDF 解析 | Python pdfminer.six（不用 pdf-parse JS）| pdf-parse ESM 衝突、pdfminer 已驗證 |
| TS 翻譯方法 | 純 TS Array + Map | 避免 polars/danfojs 數字格式化差異 |
| 日期解析 | 手動 regex + Date.UTC | 避免本地時區影響小時欄位 |
| CSV 輸出 | 手動拼接（UTF-8 BOM + CRLF）| 最大控制力，確保 byte-level match |

### 後續行動

- Lead 驗收 M3+M4+M5（本事件）
- M6 + M7（endpoint 抽象 + `tdcs-dl config` 4 subcommand）為第三批派工
- M5 gpt5_worker fallback 已不需要（PASS）

---

## [2026-06-02 20:30] 事件 #4 [Lead+User] PLAN_E6 / M3 二次驗收 — User 抓 2 漏 + Lead 修補 + 計畫派 opus 階段審查

### 背景

事件 #3 [sonnet] M3+M4+M5 PASS 後、Lead 抽樣 12 個 gantry 給 User 翻 PDF 對。**User 自己另外用獨立工具做最嚴格全表逐筆比對、抓到 Lead 抽樣沒看到的兩個問題**：

1. **JSON 漏 2 個 ramp gantry**：`05FR113S` + `05FR143N`（國 5 表 12 最後一列、宜蘭(北)↔宜蘭(南) 連絡匝道）。sonnet ingest 從 ai_workspace v3.4 base 起跑、v3.4 也漏 FR、v4.1 PDF 明列、sonnet 沒察覺。
2. **4 個錯誤 note「手冊未列」**：`01F0153S/N`（東湖↔內湖）+ `01F3535S/N`（岡山↔楠梓(北)）— 標 note「手冊未列的細部偵測點」但 v4.1 PDF 表 8 明列、note 是 v3.4-era 殘留、與 v4.1 reality 矛盾。

Lead 抽樣 12 個（含麻豆 4 + 路段首尾 + 非對稱列 + FR 代表）= 對 FR 有 cover、但 sonnet 真的把它漏掉、Lead **沒實際 cross-check sonnet 是否抽到**。Lead 自我檢討：抽樣覆蓋率不足、應該逐筆 count audit、不只看 12 個樣本。

### 目標

1. 二次驗證 User 修正 JSON 正確性
2. 修補 cli/data/gantries_v4_1.json：補 2 個 FR + 清 4 個錯誤 note
3. 更新 diff report 反映 v4.1 = 339（不是 337）
4. 清理 User 下載 + temp file
5. 設計 opus_worker 階段審查 onboarding prompt（解決「Lead 抽樣覆蓋率不足」根因、找 PLAN_E5+E6 還有沒有類似漏網）

### 處理步驟

1. **讀 User 修正版 fixed JSON**（339 個）+ orig（337 個）、用 Python diff 驗證
2. **diff 結果**：
   - Added 2: 05FR113S + 05FR143N（route=國5、section 對齊 PDF、county/city=宜蘭縣/宜蘭市 推定）
   - Removed 0
   - 既有 337 entries：section / note 全 0 diff
3. **Lead 補修 4 個錯誤 note**（User 留給 Lead 決定）：01F0153S/N + 01F3535S/N、note 清空
4. **寫回 cli/data/gantries_v4_1.json**（339 entries、4 個錯誤 note 已清）
5. **驗證 final**：per-route count = 國1 150 / 國1H 15 / 國3 156 / 國3甲 4 / **國5 14** = 339；FR 兩個有 note「請確認 county/city」；4 個 cleared note 為 ""
6. **update diff report**：v4.1 總數 337 → 339、新增 0 → 2、補 §「新增門架（v3.4 無、v4.1 有）」
7. **cleanup**：刪 `C:/Users/USER/Downloads/gantries_v4_1_fixed.json` + 刪 `cli/data/_verify_diff.txt`（temp）

### 結果與數據

| 項目 | 修補前 | 修補後 |
|---|---|---|
| 總 gantry 數 | 337 | **339** |
| 國 5 | 12 | **14** |
| FR (ramp) gantries | 0 | **2**（FR113S + FR143N）|
| 錯誤 note「手冊未列」 | 4 | **0** |
| diff report v4.1 總數 | 337 | **339** |
| diff report 新增 | 0 | **2** |
| User download cleanup | 在 | **已刪** |
| temp _verify_diff.txt | 在 | **已刪** |

### Lead 自我檢討（§0.3 4 步流程）

**違規**：Lead 抽樣驗收（12 個 gantry）覆蓋率不足、漏抓 sonnet ingest 的 FR113/143 漏洞。

**根因**：Lead 預設 sonnet count 國 5 = 12 等於 v3.4 = 12 = OK、沒進一步問「v3.4 自己對嗎？」。應該對 PDF 做全表逐筆 audit、不依賴 v3.4 作 ground truth。

**4 步補救**：
- 妥協紀錄寫進本事件 ✅
- 修補項已落實（339 entries + diff report + cleanup）✅
- 風險條目補進 MASTER_PLAN §7（後續加）
- **設計 opus_worker 階段審查機制**（見 後續行動）

**目的**：避免 PLAN_E7~E13 重蹈覆轍 — 引入 opus 作獨立階段審查、不依賴 Lead 抽樣。

### 後續行動

- **派 opus_worker 階段審查**（完整 onboard、第一次派、~3-4 hr）
  - 範圍：PLAN_E5 全部產物 + PLAN_E6 M1-M5 產物 + cli/ codebase + briefs
  - 目標：找類似 FR 漏的「數據完整性 / 邏輯矛盾 / 跨檔一致性 / 鎖板對齊 / §0.3 違規」問題
  - 產出：structured findings report 分嚴重度 + 修補建議
- opus 報告審完後、回頭派 sonnet 修 + 派 sonnet 第三批 M6+M7
- M3 二次驗收 PASS（339 對齊 PDF、4 錯誤 note 清乾淨）

---

## [2026-06-02 23:55] 事件 #5 [opus_worker] PLAN_E5+E6 階段審查 — 1 Critical + 3 High + 5 Medium findings

### 背景

事件 #4 Lead 自我檢討「抽樣覆蓋率不足、漏抓 sonnet ingest 的 FR113/143」後、決定引入獨立階段審查機制（不依賴 Lead 抽樣）。Lead 第一次派 `opus_worker`（Claude Opus 4.8、⏳ 待派 → 本次完整 onboard）、範圍 = PLAN_E5 全部 + PLAN_E6 M1-M5 + cli/ codebase + 3 briefs + 期末規則 + 手冊 v4.1 PDF、8 條維度（數據完整性 / 邏輯矛盾 / 跨檔一致性 / 鎖板對齊 / §0.3 工程合理性·邏輯正確性·長期維護 / 紀錄完整性），找類似 FR 漏的「抽樣 + 自驗都漏」問題。**只報告、不修、不動 production 檔、不 commit、不派子代理（§5.0）**。

### 目標

逐筆讀 22 份檔、cross-check、產出 structured findings report 分嚴重度 + 修補建議；獨立用 pdfminer 重抽 §3 全表對 gantry JSON 逐筆驗證；對 tdcs-clean.ts 邏輯 vs PLAN/brief spec 做對照。

### 處理步驟

1. 依 Tier 1~7 順序讀完 22 份檔（workflow 主幹 + PLAN_E5 封存 + 3 briefs + 期末規則 dump + PLAN_E6 + 實驗紀錄6 + cli/ 源碼 + 跨區 reference）。
2. **gantry 完整性硬驗**：pdfminer 重抽 PDF §3、抽出 339 個 gantry-like token、與 `gantries_v4_1.json` 339 IDs 做 set 比對 = **完全相等（零多零漏）**；確認 ingest 腳本 regex `\d{2}[A-Za-z]\d{4}[NS]` 唯一漏的就是 FR113/143（其他 4 路段無 ramp 漏網）。
3. **清洗邏輯對照**：tdcs-clean.ts `cleanRawDf` vs PLAN_E6/M4 spec line 125 vs brief_cleaning_arch §2 — 發現 core 只做 O OR D、未做 spec 要求的 trip_information 嚴格穿越、且文件宣稱有做。
4. cross-file grep（345/339/337、嚴格穿越/O OR D、worker 數）定位 stale 字樣與矛盾。
5. 對期末規則 R11/R13/R15-17 + deadline 6/8~6/10 vs roadmap / PLAN_E13 / §0.3 4 步流程做對齊檢查。
6. 寫 `workflow/reports/brief_stage_audit_2026-06-02.md`（§0 summary + §1-§5 findings + §6 對齊矩陣 + §7 評估）。

### 結果與數據

| 嚴重度 | 數量 | 代表 finding |
|---|---|---|
| Critical | 1 | F-C1 tdcs-clean.ts 只做 O OR D、未實作 spec 要求的嚴格穿越篩（trip_information regex）、文件全篇宣稱有做、M5 baseline 結構上抓不到 |
| High | 3 | F-H1 ingest 腳本 regex 抓不到 FR、重跑覆寫 339→337；F-H2 §0.3 4 步妥協第 3 步（MASTER_PLAN §7 登記）未做 + brief_requirements 缺檔；F-H3 roadmap/PLAN_E13 未對映規則 11 章節 + 無 6/10 fallback |
| Medium | 5 | diff report 國5=12/總覽 339 矛盾；gantries JSON 未接進 TUI + 「345」stale；全域 v4.1=345 錯（應 339）+ PLAN_E6 驗收「≥345」不可能；Glue 計分風險未登記 §7；3 vs 4 worker 名冊未對齊 |
| Low | 2 | package.json dead deps（pdf-parse/csv-parse/figlet）；Gantry.tsx 顯示名稱違 D8 |
| Note | 4 | utf8 vs Big5 / INDEX stale / diff 重編號 narrative / 報告 §編號對映 |

**正面確認**：gantries_v4_1.json 339 = PDF §3 完全相等（資料底子穩）；M5 baseline 37/37 md5 真 PASS；TS 翻譯（純 Array+Map）工程品質高、註解完整。

**整體就緒度（進 PLAN_E7 前）：6.5/10**。

### 後續行動

- **Lead 優先拍板 F-C1**：產品要不要做嚴格穿越篩（選項 A 維持 O OR D + 全域改措辭、撤雪山 case；選項 B 補 crossing 模式 + 雪山 baseline gate）——這是設計決策、決定 tdcs-clean.ts / Lambda handler / PLAN_E12 是否成立。**建議 F-C1 釐清前不開 PLAN_E7**。
- 修 F-H1 ingest 腳本（regex 放寬 + FR/note patch 內建、讓 339 可重生）+ 順手校正 F-M1/F-M3 數字。
- 補 F-H3 的 MASTER_PLAN §7 兩條風險 + 產出 brief_requirements + 與 User 定 6/10 fallback 交付切片。
- F-M5 / F-L1 / F-L2 併下次 sonnet/haiku 續派順手清。
- 報告全文：[`reports/brief_stage_audit_2026-06-02.md`](reports/brief_stage_audit_2026-06-02.md)。
- 紅線未觸發（F-C1 不影響 E6 跳板 winner、非制度性 systematic、22 檔皆可讀）、審查完整收尾。

---

## [2026-06-02 21:00] 事件 #6 [Lead+User] PLAN_E6 / opus audit 後 — F-C1+H2+M4 Lead 自做修補完成

### 背景

事件 #5 opus_worker 階段審查完成、出 1 Critical + 3 High + 5 Medium + 2 Low + 4 Note。Lead + User 對 F-C1 拍板：**選項 A 變體** — 不是「保留嚴格穿越措辭」、是**「砍掉整個嚴格穿越概念」**（User 質疑「我們為什麼要說什麼嚴格穿越」、發現是 Lead 過度設計）。

User 確認「mcp 理想跟一開始一樣」、純文字工作不動 code、整套修補回到 6/2 pivot 鎖板那一刻。

### 目標

Lead 自做核心修補（最高污染風險、不派 worker）：
1. F-C1：砍 active 檔所有「嚴格穿越」措辭、撤 PLAN_E12 雪山 case、保留歷史檔不動
2. F-H2 + F-M4：補 MASTER_PLAN §7 三條風險（Lead 跨工作區污染 / 抽樣覆蓋率不足 / Glue 計分張力）
3. F-H2 + F-H3：產出 brief_requirements_2026-06-02.md 釘規則 11 章節 + 6/10 fallback 切片

### 處理步驟

1. **grep 確認影響範圍**：`嚴格穿越` 在 5 個 active 檔出現 ~20 處（MASTER_PLAN 9 / PLAN_E6 3 / brief_data_inventory 5 / brief_cleaning_arch 2 / Data.tsx 1）
2. **F-C1 修補 21 處 edit**：
   - PLAN_E6.md：line 23 / 123 / 125 — 改 spec 措辭為「O/D 端點命中篩」、line 125 移除「+ 嚴格穿越（trip_information regex）」
   - MASTER_PLAN.md：line 17 / 83 / 129 / 153 / 230 / 263 / 297 / 387 / 488 — 全面改成「O/D 端點命中篩」+ §11 changelog reframe「兩大陷阱」（不是「三大陷阱」）
   - PLAN_E12 雪山 case：line 297 撤、改 3 個短距離通用化 case（麻豆 + 任意路段 A + 任意路段 B）
   - brief_data_inventory：line 20 / 65 / 165 / 178 / 235 / 248 — 標註「ai_workspace 雪山專用、mcp 不導入」、reframe「不能用 O OR D」warning 為「mcp 場景 O OR D 是對的、長距離才需嚴格穿越（非 mcp scope）」
   - brief_cleaning_arch §0：line 9-19 整段 rewrite「業務 spec 對比」、結論「兩套不同 spec、不應對齊」；line 66：「嚴格穿越篩」→「O/D 端點命中篩」
   - Data.tsx line 13：UI desc 改「O/D 端點命中篩、適合短距離路段流量研究」
   - 保留：§1 ai_workspace 描述（line 41/55 等、寫 ai_workspace 自己的事、正確）
3. **F-H2 + F-M4 §7 風險登記**：補 3 條新風險（line 411-413）
4. **brief_requirements_2026-06-02.md 出版**：
   - §0 規則總覽 + Deadline 序
   - §1 11 章節對映表（內容 / 素材 / 責任 / 狀態）
   - §2 PLAN 對映（PLAN_E5~E13 → 章節）
   - §3 風險登記（R1 Glue / R2 8 天 deadline / R3 User 任務 / R4 GoogleMap）
   - **§4 6/10 Fallback 切片**（Tier 1 已完成 / Tier 2 必交 / Tier 3 nice-to-have + 最壞情境保底版預估 60-65%）
   - §5 特殊規則（Cloud9 不用的 narrative + 互評 + 會議）
5. **保留歷史檔不動**：紀錄6 事件 #1-#5、brief_stage_audit、logs/*、ai_workspace 全部不動

### 結果與數據

| 修補項 | 落地 |
|---|---|
| F-C1 active 檔「嚴格穿越」清乾淨 | ✅ 21 edits / 5 files、grep 殘留只在 ai_workspace 上下文標註 |
| PLAN_E12 雪山 case 撤、改 3 短距離通用化 case | ✅ MASTER_PLAN line 297-300 |
| F-H2 MASTER_PLAN §7 補 3 條風險（line 411-413）| ✅ Lead 跨工作區污染 + 抽樣覆蓋率 + Glue 計分張力 |
| F-M4 Glue 計分張力風險登記 | ✅ 合進 §7 第三條 |
| brief_requirements 出版 | ✅ ~280 行、含 11 章節對映 + 6/10 fallback 切片 + Tier 1/2/3 |
| tdcs-clean.ts code 不動 | ✅ 邏輯對麻豆是對的、不需動 |
| ai_workspace 不動 | ✅ 它對雪山是對的、不污染 |

### 後續行動（給 sonnet 第三批 + User）

- **sonnet 第三批已完成**：F-H1/M1/M2/M3/M5/L1 全 PASS（事件 #7）
- **F-L2 / F-N1-N4** 不處理（deferred 到 PLAN_E11 / 觀察項）
- 下一步：Lead 派 sonnet 繼續 M6+M7（config endpoint 抽象 + `tdcs-dl config` subcommand），然後可開 PLAN_E7
- User 任務（GoogleMap 截圖 / 會議紀錄 / 心得 / 互評）依 brief_requirements §4 Tier 2 排程

---

## [2026-06-02 24:00] 事件 #7 [sonnet_worker] PLAN_E6 / opus audit 修補批次 — F-H1/M1/M2/M3/M5/L1 全 PASS

### 背景

opus_worker 完成 PLAN_E5+E6 階段審查（事件 #5），Lead 自做 F-C1 spec 拍板（O OR D 是麻豆正確語意、不補嚴格穿越）+ F-H2/H3（§7 風險 + brief_requirements）後，續派 `sonnet_worker` 執行剩餘 6 個 High/Medium/Low finding。

### 目標

F-H1 ingest 腳本 regex + patch dict（讓 339 可重生）/ F-M1 diff 報告國5 12→14（F-H1 自動修）/ F-M2 Gantry.tsx 接 gantries_v4_1.json 339 個門架（移除 12 placeholder）/ F-M3 全工作區 v4.1=345 stale → 339 / F-M5 MASTER_PLAN 3→4 worker / F-L1 package.json 清 dead deps。

### 處理步驟

1. **F-H1**：`ingest_gantries_v4_1.py` regex 改為 `\d{2}[A-Za-z]{1,2}\d{3,4}[NS]`（涵蓋 FR ramp）；加 `NOTE_CORRECTIONS`（清 4 個舊 note）+ `FR_GANTRY_DEFAULTS`（FR 推定 county/city）dict；整合進 `build_json()`；重跑產出 339 門架（國5=14）。
2. **F-M1**：diff report 隨 F-H1 腳本重跑自動更新（國5:12→14）。
3. **F-M2**：`Gantry.tsx` 移除 12 個 placeholder const；改 `import rawGantries from '../../../data/gantries_v4_1.json'`；map JSON schema（gantry_id→id）；UI 文字「完整 345 個門架於 M3 版本更新」→「全台 339 個門架，TDCS 手冊 v4.1」。
4. **F-M3**：context-aware 修 4 個文件的 v4.1=345 stale：
   - `MASTER_PLAN.md` §1.2（339）+ §4 PLAN_E6/M3（339）
   - `PLAN_E6.md` 目標段（339）+ M3 sub-table（339）+ 驗收標準 `≥345` → `= 339`
   - `實驗紀錄6.md` line 98 妥協紀錄（345→339）
   - `logs/`、`brief_data_inventory`（v3.4=345 正確）、`brief_stage_audit`（描述 bug，不動）—— 全部保留。
5. **F-M5**：`MASTER_PLAN.md` §0 worker 欄（三→四）+ §6 D13（3 固定→4 固定 + haiku_worker）+ §8.3 表（新增 haiku_worker 列）。
6. **F-L1**：`package.json` 移除 `pdf-parse` / `csv-parse` / `@types/figlet`（dead deps）、`@types/csv-parse` / `@types/pdf-parse`（移至 devDependencies 或刪）；重跑 `npm install + build + test`。

### 結果與數據

| Finding | 狀態 | Before → After |
|---|---|---|
| F-H1 ingest regex | ✅ PASS | 重跑 = 337 → 重跑 = 339；FR gantry `05FR113S`/`05FR143N` 進 JSON；4 個錯誤 note 清空 |
| F-M1 diff 國5 | ✅ PASS | 12 → 14（腳本重生後自動正確）|
| F-M2 Gantry.tsx | ✅ PASS | 12 placeholder → 339 JSON import；build 1531 KB；wizard 正常啟動 |
| F-M3 全域 345→339 | ✅ PASS | MASTER_PLAN 2 處、PLAN_E6 3 處、實驗紀錄6 1 處；v3.4=345 全保留 |
| F-M5 3→4 worker | ✅ PASS | MASTER_PLAN §0 + §6 D13 + §8.3 表 全補 haiku_worker |
| F-L1 dead deps | ✅ PASS | 移除 pdf-parse/csv-parse/@types/figlet/type overlaps；npm install + build 1531 KB + 28/28 test PASS |

**整體驗收**：
- `npm run build` ✅ 1531 KB
- `npm test` ✅ 28/28 PASS
- `npm run typecheck` ✅ 0 errors
- Wizard 非 TTY 啟動正常

### 後續行動

- Lead 驗收本批 6 個 finding
- 下一批：M6（config endpoint 抽象）+ M7（`tdcs-dl config` 4 subcommand）
- F-L2 / F-N1-N4 deferred（PLAN_E11 / 觀察項）

---

## [2026-06-02 21:30] 事件 #7 [Lead] Phase 3 — sonnet 第三批 audit 修補 Lead 抽驗 PASS

### 背景

事件 #6 後派出 sonnet 第三批 6 個 finding（F-H1 / F-M1 / F-M2 / F-M3 / F-M5 / F-L1）。sonnet 回報全綠：npm build 1531 KB、test 28/28、typecheck 0 errors。Lead 按 §0.3 紀律與「抽樣覆蓋率不足」教訓，獨立抽驗、不只信 worker 自稱。

### 目標

抽驗 6 個 finding 的硬產出（檔案 / 數字 / 結構）對齊回報、確認本批可關。

### 處理步驟

1. **F-H1 / F-M1**：`python -c json.load gantries_v4_1.json` → 339 筆、按 route Counter：國1=150 / 國1H=15 / 國3=156 / 國3甲=4 / **國5=14** ✅；FR113S / FR143N 兩筆 sample 含 county/city/note 三欄齊全（非 null）
2. **F-M2**：`Read Gantry.tsx` 1-40 → `import rawGantries from '../../../data/gantries_v4_1.json'` 在 line 5、map gantry_id→id 在 line 21-23、placeholder 全砍 ✅
3. **F-L1**：`Read package.json` → dependencies 砍剩 10 條（aws-sdk × 3 + chalk / cli-progress / commander / gradient-string / ink / ora / react），無 pdf-parse / csv-parse；devDependencies 含 @types/* + esbuild / jest / ts-* / typescript ✅
4. **F-M3**：grep `\b345\b` → MASTER_PLAN.md:85 含 339 + v3.4=345 註記 ✅、PLAN_E6.md:97/109 339 ✅、實驗紀錄6:98 339 ✅、logs/ 內 v3.4=345 保留不動 ✅、brief_data_inventory:23 描述 v3.4 config/gantry_to_county.json 345（場景對、不動）✅
5. **F-M5**：grep `haiku_worker` MASTER_PLAN.md → §0 line 32 worker 名冊 4 個 ✅、§6 D13 line 392 含 D13 條目 ✅、§8.3 line 436 haiku_worker 列 ✅

### 結果與數據

| Finding | sonnet 自稱 | Lead 抽驗 | 差距 |
|---|---|---|---|
| F-H1 339 / 國5=14 | ✅ | ✅ json count 339、Counter 國5=14 | 0 |
| F-H1 FR ramp county/city/note | ✅ | ✅ sample FR113S / FR143N 三欄齊全 | 0 |
| F-M1 diff 國5=14 | ✅（隨 F-H1） | ✅（隨 F-H1 自動） | 0 |
| F-M2 Gantry.tsx 接 JSON | ✅ | ✅ line 5 import + line 21-23 map | 0 |
| F-M3 v4.1=345 → 339 | ✅ MASTER 2 處 / PLAN 3 處 / 紀錄6 1 處 | ✅ 全對；v3.4=345 全留 | 0 |
| F-M5 3→4 worker | ✅ §0+§6+§8.3 | ✅ 三處 line 32 / 392 / 436 | 0 |
| F-L1 dead deps + build/test | ✅ build 1531 KB / 28/28 | ✅ package.json 砍對；build/test 信任 sonnet 跑（未重跑） | 0 |

**抽驗結論**：6/6 PASS、無 spec drift / 無漏改 / 無 stale 殘留。

### 後續行動

- PLAN_E6 M1-M5 + 三批 audit 全綠燈 → 可進 M6+M7（endpoint 抽象 + `tdcs-dl config` subcommand）
- 待 User 確認啟動 M6+M7 派工 prompt（sonnet 或 Lead 自做、Lead 推 sonnet）
- M6+M7 完 → 跑 `close_plan.py --key-result --next-title` 一鍵封存 → 進 PLAN_E7（AWS infra setup）

---

## [2026-06-02 22:10] 事件 #8 [Lead] Phase 2 — M6 endpoint 抽象 + M7 `tdcs-dl config` 4 subcommand

### 背景

PLAN_E6 三批 audit 完、剩 M6 + M7。User 選 Lead 自做（< 5 檔、設計即實作、不浪費派 sonnet overhead）。M6 / M7 spec 已在 PLAN_E6.md line 168-206 寫定：三層 priority（env > file > default）+ 4 subcommand（set-endpoint / get-endpoint / show / reset）+ URL 驗證。

### 目標

- 把 `cli/src/index.ts` 內的 4 個 config stub 換成真實實作
- 引入 `~/.tdcs-dl/config.json` + `TDCS_DL_ENDPOINT` env override
- 加單元測試 + integration scenario 走完
- typecheck / jest / build 三項全綠

### 處理步驟

1. **M6 核心 lib** — `cli/src/lib/config.ts`（~120 行）：
   - `resolveEndpoint()` 三層 fallthrough、回 `{value, source}`
   - `setEndpoint(url)` URL 驗證 + merge 既有欄位 + 原子寫（temp + rename）
   - `loadConfigFile()` 容錯（檔缺 / JSON 壞 → null）
   - `validateEndpointUrl()` 限 http(s)、拒 ftp / javascript / 非 URL
   - `getShowView()` 給 `show` subcommand 用、每欄附 source
   - **內部 env hook** `TDCS_DL_CONFIG_DIR`：CI / test 重定向 config dir、不污染 user `~/.tdcs-dl/`（git / npm / docker 同 pattern、§0.3 合理擴展不算 hack）

2. **M7 commander register** — `cli/src/commands/config.ts`（~70 行）：
   - export `registerConfigCommands(program)`、把 4 個 action 從 lib 取資料、純 IO 層
   - set-endpoint 印 file 位置 + 偵測 `TDCS_DL_ENDPOINT` 已設時警告會 override
   - get-endpoint 印 `endpoint = ... / source = ...`
   - show 印 `config file path / endpoint [source] / profile [source]`
   - reset 偵測檔不存在 → `(no-op)` 提示、不爆

3. **index.ts 改線**：4 個 stub 段（~30 行）→ `registerConfigCommands(program)` 一行

4. **單元測試** — `cli/tests/config.test.ts`（18 tests / 7 describe）：
   - 三層 priority 5 case（no/file/env/env-beat-file/empty-env-fallthrough）
   - setEndpoint round-trip（merge 既有 profile 不 clobber）
   - validateEndpointUrl（http/https/garbage/ftp/javascript 5 case）
   - resetConfig（remove + no-op）
   - loadConfigFile 壞 JSON 容錯
   - getShowView
   - 每 test `beforeEach` 用 `mkdtempSync` + 設 `TDCS_DL_CONFIG_DIR` 隔離；`afterEach` rm tmpDir + restore env

5. **README** — `cli/src/lib/README.md`：寫設計 rationale、三層 priority 圖、為什麼選 `~/.tdcs-dl/`（npm / aws / docker 慣例）、env var 一覽、subcommand → lib 函式對映、安全邊界、後續 PLAN_E7 改 default URL

6. **9 個 integration scenario 走完**（dist/index.js direct invoke）：
   1. no file → default ✅
   2. set-endpoint 寫檔 ✅
   3. get-endpoint = file source ✅
   4. `TDCS_DL_ENDPOINT=...` override → env source ✅
   5. show 印 file path + endpoint [file] + profile [none] ✅
   6. reset 刪檔 ✅
   7. reset 後 get → default ✅
   8. 非法 URL `notaurl` → exit code 1 + 錯誤訊息 ✅
   9. reset 兩次 → no-op 提示 ✅

### 結果與數據

| 指標 | 數值 |
|---|---|
| 新增檔案 | `lib/config.ts` / `commands/config.ts` / `tests/config.test.ts` / `lib/README.md` |
| 修改檔案 | `src/index.ts`（30 行 stub → 1 行 register） |
| 新增程式碼 | lib 124 行 / commands 70 行 / tests 153 行 / README 60 行 |
| typecheck | ✅ 0 errors |
| jest | ✅ **46/46 PASS**（28 baseline + 18 config） |
| build | ✅ dist/index.js 1534 KB |
| integration | ✅ 9/9 scenario 通過 |
| 三層 priority | ✅ env > file > default 完全對齊 spec |
| URL 驗證 | ✅ 拒 ftp / javascript / 非 URL |
| 跨平台 | tmp dir 用 `os.tmpdir()` / 路徑用 `path.join`、Windows 環境驗測 PASS |

### 後續行動

- **PLAN_E6 完成定義 7 條全綠**（M1 ✅ / M2 ✅ / M3 ✅ / M4 ✅ / M5 ✅ / M6 ✅ / M7 ✅）
- 待 User 拍板 → 跑 `python D:\p\workflow\scripts\close_plan.py --key-result "<...>" --next-title "AWS infra setup"`
- Lead 將擬 PLAN_E7 草稿（Terraform + API GW + Lambda + S3 + Athena + Glue Data Catalog；對齊 brief_cleaning_arch_comparison §3 + brief_requirements §2 PLAN_E7 對映）
- close_plan 前 Lead 先擬「關鍵成果」一行（建議：「v2 CLI 骨架完成 + tdcs_clean TS 翻譯 baseline 14,058 行 md5 對齊 + endpoint 抽象機制就位，PLAN_E7 AWS infra 可接管」）



