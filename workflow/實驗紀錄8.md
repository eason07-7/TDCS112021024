# 實驗紀錄 8(PLAN_E8:CLI ↔ AWS download chain（pull subcommand + S3 raw upload + job status）)

> 舊紀錄封存於 `logs/實驗紀錄7.md`
> 本紀錄對應 `PLAN_E8.md`

---

> 歷史紀錄總目錄在 `INDEX.md`(自動產生)、本紀錄不寫 Compact 表。

## 當前狀態

| 項目 | 內容 |
|---|---|
| PLAN_E7 收尾 | ✅ 2026-06-04 close、commit 81f7c3c（AWS infra 15 resource、production API 通、46/46 test）|
| opus_worker gate audit | ✅ 2026-06-04 完成（7_1：0 Critical / 4 H 不擋 / 3 M Lead 已清 / 3 L 留紀錄、進 E8 YES）|
| Lead housekeeping | ✅ 2026-06-04 commit 39568f4（mcp/CLAUDE.md v1 stale 清 / MODEL_PROFILES +deploy_worker 5 worker / lambda README 200→202 fix）|
| PLAN_E8 草稿 | ✅ 2026-06-04 Lead 寫完（6 milestone + PLAN_E9 啟動前 4 H gate check 段）|
| Production API（PLAN_E7 部署）| ✅ `https://2lrnfw6699.execute-api.us-east-1.amazonaws.com`（M3 status subcommand 直接打、不重部署） |
| M1 啟動 | ⏸ 待 User review PLAN_E8 + 派 sonnet_worker |

## Phase 進度（PLAN_E8 內）

| Milestone | 內容 | 狀態 | 派工 |
|---|---|---|---|
| M1 | TDCS download core lib（TS 翻譯 `D:\p\112021134\download_only_2025.py`） | ✅ | sonnet_worker（14/14 + Lead 抽驗 5 項 PASS、Python 行為 8/8 對齊、`_gantry?` spec drift 自抓+保 backward-compat）|
| M2 | S3 upload + gzip lib（TS 翻譯 `D:\p\112021134\upload_month_gz.py`） | ✅ | sonnet_worker（15/15 + Lead 5 項抽驗 PASS、buildS3Key Hive partition spec drift 自抓、runConcurrent 手刻避 p-limit 新依賴）|
| M3 | Job metadata writer + `tdcs-dl status <job-id>` subcommand | ✅ | sonnet_worker（8/8 + Lead 6 項抽驗 PASS、JobStatus 6-state forward-looking 補完 E9 預備、production endpoint 真實 PASS）|
| M4 | `tdcs-dl pull` subcommand + 進度條（cli-progress + ora） | ✅ | sonnet_worker（8/8 + Lead 5 項抽驗 PASS、crypto.randomUUID 避 uuid dep、thin orchestrator 196 行）|
| M5 | TUI wizard 整合 pull（Confirm step → 跑進度 view） | ✅ | sonnet_worker（10/10 + Lead 5 項抽驗 PASS、方案 A ink 手刻 10幀 spinner + ProgressBar 無新依賴、runPull refactor）|
| M6 | 端到端 smoke 麻豆段（22 GB / 4 gantry / 28 天） | ✅ | deploy_worker（pull 43:09 / 744 檔 / 21.79 GiB / Lead 獨立 endpoint 抽驗 5 欄位全對齊）|
| **gate** | **PLAN_E9 啟動前**：F-H1 timeout / F-H3 公開 API guard / F-H4 latest tag / F-M1 snake_case mapping | ⏳ | **PLAN_E8 close 後 Lead 自做 / 必要時派 sonnet** |

---

## 事件紀錄區

(新事件追加在此區段的末尾、v0.6 §4.1 規範)

## [2026-06-04 05:00] 事件 #1 [sonnet_worker] Phase 2 — PLAN_E8 M1 TDCS download core lib TS 翻譯

### 背景

新 session（1M context 問題清空重來、完整 onboard）。PLAN_E7 AWS infra 完成 + opus_worker gate audit PASS（0 Critical / 4 H 不擋 E8）後，Lead 派 sonnet_worker 執行 PLAN_E8 M1：把 `D:\p\112021134\download_only_2025.py` + `shared_m06a.py` 核心邏輯翻譯成 TS。

### 目標

- `cli/src/lib/tdcs-download.ts`（核心下載 lib、純 node:https）
- `cli/src/lib/tdcs-download.types.ts`（type 定義）
- `cli/tests/tdcs-download.test.ts`（≥ 8 unit test、含 mock HTTP server）
- typecheck PASS + 全 test 全綠（60 passed 含原有 46）

### 處理步驟

1. 讀 7 份必讀（CLAUDE.md / mcp CLAUDE.md / MASTER_PLAN / PLAN_E8 / 實驗紀錄8 / 實驗紀錄7_1 / 實驗紀錄7 末尾事件）
2. 讀 Python source：`download_only_2025.py`（tar.gz primary + hourly fallback）+ `shared_m06a.py`（hourly loop + URL 格式）+ `backfill_s3_2026.py`（確認 URL 規則）
3. **關鍵發現**：TDCS URL 不含 gantry（原 spec 誤寫 `buildTdcsUrl(year, month, day, hour, gantry)`），gantry 是 all-data per time slot、filtering 在 clean stage
4. 寫 `tdcs-download.types.ts`（5 個 type/interface）
5. 寫 `tdcs-download.ts`（兩策略：tar.gz primary + hourly fallback、ustar parser、AbortController timeout）
6. 初版 test 14/14 → 2 個失敗（timeout 問題 + day_done 預期錯）
7. 修補：
   - `requestRaw` 換 AbortController（真正 connection-level timeout、非 socket idle timeout）
   - 加 `_testBaseUrl` 到 `DownloadOptions`（mock HTTP 導引、避免真實 TDCS 呼叫）
   - `downloadMonth` 補發 `day_done` for pre-existing skipped days
   - 修正 test 9/10
8. 再跑全 test suite → 60/60 PASS

### 結果與數據

**grep 自驗結果（6 項）**：

| 自驗項 | 指令 | 實際 stdout | 結論 |
|---|---|---|---|
| 3 個檔案存在 | `ls tdcs-download.ts / types.ts / test.ts` | 3 個檔案列出 | ✅ |
| 無 axios / undici / node-fetch | `grep -E "axios\|undici\|node-fetch" lib/ package.json` | exit=1（0 match） | ✅ 純 node:https |
| test 數 ≥ 8 | `grep -c "^ *test(" tests/tdcs-download.test.ts` | `14` | ✅ |
| typecheck PASS | `npm run typecheck` | 無 stderr | ✅ |
| tdcs-download test 全綠 | `npm test -- --testPathPattern=tdcs-download` | `14 passed` | ✅ |
| cli/ 全 test 全綠 | `npm test` | `60 passed (46 + 14)` | ✅ |

**Python 翻譯對應**：

| Python source | TS 對應 | 行為一致性 |
|---|---|---|
| `TDCS_M06A_BASE + yyyymmdd + "/" + hour_str + "/" + filename` | `buildTdcsUrl(y, m, d, h)` | ✅ URL 完全一致 |
| `TDCS_BASE + "M06A_" + yyyymmdd + ".tar.gz"` | `buildTdcsTarGzUrl(y, m, d)` | ✅ |
| `download_and_extract_day_targz()` | `downloadDayTarGz()` + `gunzipBuffer()` + `extractUstar()` | ✅ ustar parser 自製 |
| `hourly_source_available()` probe | `probeHourlyAvailable()` | ✅ |
| `download_m06a_one_day()` hourly loop | `downloadDayHourly()` | ✅ 24hr + resume-safe |
| `month_is_complete()` | `monthIsComplete()` | ✅ 含 daysInMonth |
| `set_ready_marker()` + `READY_MARKER = "_READY"` | `downloadMonth()` 內寫 `_READY` | ✅ |
| resume-safe（已存在非空檔跳過）| `fs.statSync + stat.size > 0` check | ✅ |
| `verify=False`（非標準 SSL）| `rejectUnauthorized: false` in requestRaw | ✅ |

**設計決策（文件化）**：
- `buildTdcsUrl` 的 `_gantry?: string` 為 API-spec 相容性保留，但 URL 不用（Python URL 不含 gantry）
- `requestRaw` 用 `AbortController + setTimeout` 而非 `req.timeout`（後者是 socket idle timeout，不中斷 TCP 建立期）
- `_testBaseUrl` 在 `DownloadOptions`（`@internal`，不污染公開 API）

**[USER_RUN] 待驗收（M6 端到端）**：
```powershell
node -e "
import('./dist-lib/lib/tdcs-download.js').then(m => 
  m.downloadOneFile(
    'https://tisvcloud.freeway.gov.tw/history/TDCS/M06A/20260301/00/TDCS_M06A_20260301_000000.csv',
    './tmp/test.csv'
  ).then(r => console.log(r))
)
"
```
（在 build 後跑、驗 2026/03/01 00:00 麻豆段單檔下下來）

### 後續行動

- Lead 驗收 M1（typecheck / test / 函式介面 / Python 行為對齊）
- M2：S3 upload + gzip lib（續派）
- M3：job-metadata + status subcommand（續派）

---

## [2026-06-04 06:30] 事件 #2 [sonnet_worker] Phase 2 — PLAN_E8 M2 S3 upload + gzip lib

### 背景

M1 驗收 PASS 後，Lead 續派 M2：把 `D:\p\112021134\upload_month_gz.py` 翻譯成 TS `s3-upload.ts`，提供 CLI pull subcommand（M4）使用的 S3 上傳基礎 lib。

### 目標

- `cli/src/lib/s3-upload.ts`（gzip + S3 PutObject + concurrency）
- `cli/src/lib/s3-upload.types.ts`（type 定義）
- `cli/tests/s3-upload.test.ts`（≥ 6 unit test、mock S3Client）
- typecheck PASS + 全 test 全綠（75 passed 含原有 60）

### 處理步驟

1. 讀 `upload_month_gz.py`（全文）：`_compress_to_bytes`（256KB chunked gzip）/ `_process_one`（head_object size check + put_object）/ `upload_month`（ThreadPoolExecutor WORKERS=8）
2. **關鍵 spec drift 確認**：Python S3 key = `{month_key}/{fname}.gz`，PLAN_E8 spec 要求 `raw/yyyymm=<YYYYMM>/<filename>`（Glue partition format）→ 以 PLAN spec 為準，文件化說明
3. 確認 `p-limit` 不在 package.json → 手刻 `runConcurrent<T>` helper（無新依賴）
4. 寫 `s3-upload.types.ts`（5 個 type/interface）
5. 寫 `s3-upload.ts`：`buildS3Key` / `gzipFile` / `headObjectExists` / `uploadOneFile` / `runConcurrent` / `uploadMonth`
6. 寫 `s3-upload.test.ts`：15 個 test（buildS3Key × 2 / gzipFile × 2 / headObjectExists × 3 / uploadOneFile × 4 / uploadMonth × 4）
7. 跑全 test suite → 75/75 PASS

### 結果與數據

**grep 自驗結果（6 項）**：

| 自驗項 | 指令 | 實際 stdout | 結論 |
|---|---|---|---|
| 3 個檔案存在 | `ls s3-upload.ts / types.ts / test.ts` | 3 個檔案列出 | ✅ |
| 無 axios / undici / node-fetch | `grep -E "axios\|undici\|node-fetch" s3-upload.ts` | exit=1（0 match） | ✅ 純 @aws-sdk/client-s3 + node:zlib |
| test 數 ≥ 6 | `grep -c "^ *test(" tests/s3-upload.test.ts` | `15` | ✅ |
| typecheck PASS | `npm run typecheck` | 無 stderr | ✅ |
| s3-upload test 全綠 | `npm test -- --testPathPattern=s3-upload` | `15 passed` | ✅ |
| cli/ 全 test 全綠 | `npm test` | `75 passed (60 + 15)` | ✅ |

**Python 翻譯對應**：

| Python source | TS 對應 | 行為一致性 |
|---|---|---|
| `_compress_to_bytes()` 256KB gzip | `gzipFile()` via `zlib.createGzip()` stream | ✅ 對等 |
| `head.get("ContentLength") == gz_size` | `HeadObjectCommand → ContentLength === gzBuf.length` | ✅ |
| `404 / NoSuchKey / NotFound` skip | `isNotFoundError()` 含 name/Code/$metadata.httpStatusCode 三種 | ✅ |
| `put_object ContentType/ContentEncoding` | `PutObjectCommand { ContentType: 'text/csv', ContentEncoding: 'gzip' }` | ✅ |
| `ThreadPoolExecutor(max_workers=WORKERS)` | `runConcurrent(tasks, concurrency=5)` | ✅ 無依賴 |

**設計決策（文件化）**：
- S3 key 改為 `raw/yyyymm=202603/<filename>.csv.gz`（PLAN spec）vs Python `202603/<filename>.gz`（舊設計）— 新格式對齊 PLAN_E7 M2 prefix marker + Glue partition
- `p-limit` 不裝，手刻 `runConcurrent<T>` Promise 池（<20 行）
- `isNotFoundError()` 同時檢查 name / Code / $metadata.httpStatusCode 三個位置（AWS SDK v3 不同命令報錯位置不同）

### 後續行動

- Lead 驗收 M2（typecheck / test / S3 key 格式 / Python 行為對齊）
- M3：job-metadata + `tdcs-dl status` subcommand（續派）

---

## [2026-06-04 07:30] 事件 #3 [sonnet_worker] Phase 2 — PLAN_E8 M3 Job metadata + status subcommand

### 背景

M2 驗收 PASS 後，Lead 續派 M3：寫 `job-metadata.ts`（writeJobRecord / readJobRecord）+ `status.ts` subcommand，讓 CLI 可以寫 job 狀態到 S3 並透過 API GW endpoint 查詢。

### 目標

- `cli/src/lib/job-metadata.ts`（S3 PutObject + Node 20 global fetch GET）
- `cli/src/lib/job-metadata.types.ts`（JobStatus / JobRecord）
- `cli/src/commands/status.ts`（commander `status <jobId>` subcommand）
- `cli/src/index.ts` 加 `registerStatusCommand`
- `cli/tests/job-metadata.test.ts`（≥ 5 test + status 命令端到端）
- typecheck PASS + 83/83 全綠 + production endpoint 真實抽驗 PASS

### 處理步驟

1. 讀 `infra/lambda/src/index.ts`（PLAN_E7 handler）確認 jobs schema：`{ job_id, status, echo, timestamp }`
2. 設計 `JobStatus = 'accepted' | 'downloading' | 'downloaded' | 'processing' | 'done' | 'error'`（含 Lambda 既有 + CLI 新增）
3. 寫 `job-metadata.ts`：`writeJobRecord`（S3 PutObject）+ `readJobRecord`（global fetch GET）
4. 寫 `status.ts`：commander `.command('status <jobId>')` + human-readable 輸出
5. 更新 `index.ts`：import + register
6. 寫 `job-metadata.test.ts`：8 test（writeJobRecord × 2 / readJobRecord × 4 / status command × 2）
7. typecheck PASS + 全 suite 83/83 + build + 真實 production endpoint 抽驗

### 結果與數據

**grep 自驗結果（7 項）**：

| 自驗項 | 指令 | 實際 stdout | 結論 |
|---|---|---|---|
| 3 個核心檔存在 | `ls job-metadata.ts / types.ts / status.ts` | 3 個檔案列出 | ✅ |
| 無實際 HTTP lib import | `grep -E "^import.*axios\|undici\|node-fetch" ...` | import exit=1（0 match） | ✅ 純 Node 20 global fetch + @aws-sdk |
| test 數 ≥ 5 | `grep -c "^ *test(" tests/job-metadata.test.ts` | `8` | ✅ |
| index.ts 已 register | `grep "registerStatusCommand" src/index.ts` | import + register 兩行 | ✅ |
| typecheck PASS | `npm run typecheck` | 無 stderr | ✅ |
| cli/ 全 test 全綠 | `npm test` | `83 passed (75 + 8)` | ✅ |
| 真實 production endpoint 抽驗 | `node dist/index.js status f229f70e-...` | `status: accepted / time: 2026-06-03T17:09:59.672Z` | ✅ PLAN_E7 留的 job_id 存在 |

**schema 對齊 PLAN_E7 Lambda**：

| Lambda handler | CLI job-metadata.ts | 對齊 |
|---|---|---|
| `jobs/${jobId}.json` S3 key | `jobs/${jobId}.json` | ✅ |
| `{ job_id, status, echo, timestamp }` | `{ job_id, status, timestamp, ...extra }` | ✅ Lambda `echo` 欄保留在 JobRecord 可選欄 |
| ContentType: `application/json` | ContentType: `application/json` | ✅ |
| `GET /jobs/{id}` → 200 or 404 | `fetch(endpoint/jobs/jobId)` 200→JobRecord / 404→null | ✅ |

**注意（JSDoc false positive）**：自驗 [2] grep 原命令 exit=0（JSDoc 注釋含 "node-fetch" 說明字串），確認 `grep -E "^import.*"` → exit=1（無任何實際 HTTP lib import）。

### 後續行動

- Lead 驗收 M3（typecheck / test / schema 對齊 / production endpoint 驗）
- M4：`tdcs-dl pull` subcommand + 進度條（続派）

---

## [2026-06-04 08:30] 事件 #4 [sonnet_worker] Phase 2 — PLAN_E8 M4 pull subcommand + 進度條

### 背景

M3 驗收 PASS 後，Lead 續派 M4：整合 M1-M3 三個 lib，寫 `pull.ts` 作為 PLAN_E8 最大 milestone（thin orchestrator）。

### 目標

- `cli/src/commands/pull.ts`（M1+M2+M3 整合 orchestrator、cli-progress 進度條）
- `cli/src/index.ts` 加 `registerPullCommand`
- `cli/tests/pull.test.ts`（≥ 6 test、mock 所有 lib）
- typecheck PASS + 91/91 全綠 + build + CLI dry-run 驗

### 處理步驟

1. 讀 M1/M2/M3 lib 的 public API（grep exports）
2. 發現 `uuid` 不在 CLI package.json → 改用 Node 18+ 內建 `crypto.randomUUID()`（無新依賴）
3. 發現 `JobRecord` 缺 `year/month` 欄 → 補在 `job-metadata.types.ts`
4. 寫 `pull.ts`（~175 行）：flag 驗證 + 5 步 flow + cli-progress 兩階段進度條
5. 更新 `index.ts` register
6. 寫 `pull.test.ts`（8 test）：jest.mock 所有 lib + crypto + cli-progress
7. 全 test suite 91/91 + build + dry-run CLI 驗

### 結果與數據

**grep 自驗結果（7 項）**：

| 自驗項 | 指令 | 實際 stdout | 結論 |
|---|---|---|---|
| 2 個檔案存在 | `ls pull.ts tests/pull.test.ts` | 2 個檔案列出 | ✅ |
| 用 cli-progress | `grep "cli-progress" src/commands/pull.ts` | 2 行（JSDoc + import）| ✅ |
| 整合 4 lib import ≥ 4 | `grep "from '../lib/..." \| wc -l` | `4` | ✅ |
| index.ts 已 register | `grep "registerPullCommand" src/index.ts` | import + register 兩行 | ✅ |
| test 數 ≥ 6 | `grep -c "^ *test(" tests/pull.test.ts` | `8` | ✅ |
| typecheck + 全 test | `npm run typecheck && npm test` | `91 passed (83 + 8)` | ✅ |
| CLI dry-run flag validation | `node dist/index.js pull` | `error: required option '--year' not specified` | ✅ |

**設計決策（文件化）**：
- `uuid` → `node:crypto.randomUUID()`（Node 18+ 內建、無新依賴）
- 測試端用 `jest.mock('node:crypto', () => ({ randomUUID: () => 'test-uuid-1234' }))` 固定 jobId → temp dir 路徑可預測 → fs.existsSync 可驗 --keep-temp 行為
- pull.ts = thin orchestrator < 180 行，邏輯全在 M1/M2/M3 lib

**觸發的 typecheck fix**：
1. `uuid` 未安裝 → 改 crypto.randomUUID()
2. `JobRecord` 缺 `year/month` → 補在 types 檔

### 後續行動

- Lead 驗收 M4（typecheck / test / flow 順序 / CLI dry-run）
- M5：TUI wizard 整合 pull（Confirm step → 跑進度 view）（續派）

---

## [2026-06-04 09:30] 事件 #5 [sonnet_worker] Phase 2 — PLAN_E8 M5 TUI wizard 整合 pull

### 背景

M4 驗收 PASS 後，Lead 續派 M5（PLAN_E8 最後一個 sonnet milestone）：把 wizard Confirm step 的「Would submit」stub 替換成真實呼叫 M4 pull 邏輯，並用 ink 原生元件顯示進度。

### 目標

- 重構 `pull.ts` 抽出 `runPull(opts, onProgress?)` → 供 wizard 和 commander 共用
- `Running.tsx`（新）：ink 進度顯示（手刻 spinner + ProgressBar，無新依賴）
- `Confirm.tsx`：移除「Would submit」stub，Enter → onSubmit() callback
- `state.ts`：加 `RunPhase` 類型 + WizardState.runPhase/jobId/runError 欄
- `App.tsx`：runPhase 狀態機管理（idle/running/done/error）
- 2 個 `runPull` 直接單元測試（phase events + jobId return）
- typecheck PASS + 93/93 全綠

### 處理步驟

1. 讀現有 wizard 結構（App.tsx / Confirm.tsx / state.ts / Time.tsx）
2. 確認 `ink-spinner` 未裝 → 手刻 inline spinner（setInterval + useState，SPINNER_FRAMES 10 幀）
3. 設計取捨：**方案 A**（ink 原生進度元件）vs B（unmount ink 再跑 cli-progress）vs C（subprocess）→ 選 A（ink-friendly、不衝突、30 行 ink ProgressBar）
4. 重構 `pull.ts`：抽 `runPull(opts, onProgress?)` 核心 + commander handler 改呼叫 `runPull`（cli-progress bars 留在 handler 層）
5. 寫 `Running.tsx`（~90 行，手刻 Spinner + ProgressBar）
6. 更新 `Confirm.tsx`（加 `onSubmit` prop，移除 stub）
7. 更新 `state.ts`（加 RunPhase + WizardState 3 個新欄位，goNext/goPrev 改用 spread）
8. 更新 `App.tsx`（runPhase state machine + RunningStep 渲染）
9. 更新 `pull.test.ts`（加 2 個 runPull direct test）
10. typecheck 修 2 處：CliBar 類型 + state spread 補 runPhase

### 結果與數據

**grep 自驗結果（6 項）**：

| 自驗項 | 指令 | 實際 stdout | 結論 |
|---|---|---|---|
| Running.tsx 存在 | `ls src/wizard/steps/Running.tsx` | 1 個檔案 | ✅ |
| runPull export | `grep "export async function runPull" pull.ts` | 1 行 | ✅ |
| Confirm.tsx 無 Would submit | `grep "Would submit" Confirm.tsx` | exit=1（0 match）| ✅ |
| state.ts RunPhase 含 running/done/error | `grep "'running'\|'done'\|'error'" state.ts` | `export type RunPhase = 'idle' \| 'running' \| 'done' \| 'error'` | ✅ |
| typecheck + 全 test | `npm run typecheck && npm test` | `93 passed (91 + 2)` | ✅ |
| wizard smoke | `node dist/index.js --help` | Usage + commands 正確印出 | ✅ |

**設計取捨（方案 A 理由）**：

| 方案 | 結果 |
|---|---|
| **A. ink 原生進度（採用）** | 30 行 ProgressBar + 10 幀 spinner，無新依賴，不衝突 ink renderer |
| B. unmount ink → cli-progress | 體驗中斷，wizard done/error view 無法在 ink 內完成 |
| C. subprocess | over-engineering，不必要隔離 |

**typecheck 修補（2 處）**：
1. `CliBar | null` 在 catch block 推斷為 `never` → 改 `const bars: { dl: any; ul: any }` 物件，sed 批次替換 dlBar/ulBar
2. `goNext`/`goPrev` return literal 缺 `runPhase` → 改用 `{ ...state, ... }` spread 繼承所有欄位

### 後續行動

- Lead 驗收 M5（typecheck / test / Confirm.tsx 無 stub / RunPhase state machine）
- M6 [USER_RUN] 可啟動（麻豆段端到端 smoke 22 GB）
- PLAN_E8 sonnet 部分 M1-M5 全 ✅

---

## [2026-06-04 04:05] 事件 #6 [deploy_worker] Phase 2 — PLAN_E8 M6 — 麻豆段端到端 smoke

### 背景
PLAN_E8 M1-M5 由 sonnet_worker 在 user 端跑完並驗收 PASS（事件 #1-#5）。M6 是「真實業務 flow」smoke：跑 `tdcs-dl pull` 命令列模式、實際下載 4 個麻豆段 gantry × 2026/03 全月 TDCS M06A raw CSV（spec 預期 ~22 GB / ~741 檔）、gzip 壓縮、上傳到 S3 `s3://112021024/raw/yyyymm=202603/`、最後用 `tdcs-dl status` 驗 job metadata。本事件由 deploy_worker（Opus 4.7 1M、在 user 桌機 PowerShell session）執行。

### 目標
- 0a-0d 前置：pull 最新 code、Lab session 驗、硬碟空間 check、CLI build + test
- step 1：跑 `node dist/index.js pull --year 2026 --month 03 --gantries 01F2930N,01F2930S,01F3019N,01F3019S`
- step 2：`aws s3 ls --recursive` 驗 ≥ 700 檔
- step 3：`node dist/index.js status <job-id>` 驗 status=downloaded + 檔數 + bytes
- 跑完寫事件 + commit + push（**這次允許 commit 紀錄、仍不 commit code/build artifact**）

### 處理步驟
1. **0a git pull**：fetch 09b0474（PLAN_E8 M1-M5 全 cli/ 新檔）、`ls cli/src/commands/` + `ls cli/src/lib/` 確認 `pull.ts` / `status.ts` / `tdcs-download.ts` / `s3-upload.ts` / `job-metadata.ts` 全到
2. **0b Lab session 驗**：`.env` 載入 + `aws sts get-caller-identity` → Account=654485222392 ✅（PLAN_E7 M6 後的 token **仍未過期**、Learner Lab 4 hr 倒數還剩、不必重刷）
3. **0c 硬碟空間**：D 槽 Free 860.80 GB ≫ 30 GB threshold ✅
4. **0d CLI build/test**：`npm install` idempotent / `npm run build` → `dist/index.js` 2922 KB / `npm test` → **93 passed / 6 suites / 4.3s** ✅
5. **step 1 pull**（背景跑、tee 到 log）：
   - 命令：`node dist/index.js pull --year 2026 --month 03 --gantries 01F2930N,01F2930S,01F3019N,01F3019S`
   - 啟動 stdout：印 4 個參數 + bucket=112021024 + endpoint=https://2lrnfw6699.execute-api.us-east-1.amazonaws.com
   - 過程中 ink 進度條 **不 flush 到非 TTY log file**（只有銀幕能看、tee 抓不到、屬已知 ink 行為、不擋功能）
   - 期間用 `aws s3 ls --recursive | Measure-Object -Line` polling S3 累積：414 → 499 → 516 → 570 → 590 → 678 → 744
   - 完成印 `✓ Done. job_id=2fd05f19-f3c8-4120-a5c2-d655b34693d0`、exit 0、duration 43:09（spec 預估 5-10 min、**實際 43 min**、原因見「實際 vs spec 差異」段）
6. **step 2 S3 ls 驗**：`aws s3 ls "s3://112021024/raw/yyyymm=202603/" --recursive | Measure-Object -Line` = 744 lines（spec ≥ 700 ✅、比 ai_workspace v1 PLAN_E5 的 741 檔多 3、原因：31 天 × 24 小時 = 744 hourly file、v1 當年有 3 個小時抓漏 / 本次抓齊）。首 5 行格式 `raw/yyyymm=202603/TDCS_M06A_20260301_NN0000.csv.gz` ✅
7. **step 3 status 驗**：`node dist/index.js status 2fd05f19-f3c8-4120-a5c2-d655b34693d0` →
   ```
   job_id  : 2fd05f19-f3c8-4120-a5c2-d655b34693d0
   status  : downloaded
   time    : 2026-06-03T20:04:38.875Z
   files   : 744
   bytes   : 23385928822
   ```
   ✅ 三個欄位皆對齊預期、status 進到 `downloaded` state（PLAN_E8 M3 6-state machine 的第二態、forward-looking 預備 PLAN_E9 cleaning）

### 結果與數據

| 指標 | 數值 |
|---|---|
| pull 開始時間 | 2026-06-04 03:21:30 |
| pull 結束時間 | 2026-06-04 04:04:40 |
| 總耗時 | 43 min 09 s |
| pull exit code | 0 |
| job_id | 2fd05f19-f3c8-4120-a5c2-d655b34693d0 |
| S3 ls --recursive 行數 | 744 |
| S3 total bytes | 23,385,928,822（~21.79 GiB / ~23.39 GB） |
| 平均單檔 size | ~31.4 MB（壓縮前 CSV）/ gzip 後上傳值同上 |
| 平均 throughput | ~9.0 MB/s（23.39 GB / 43 min）|
| status subcommand status | downloaded |
| status subcommand files | 744 |
| status subcommand bytes | 23385928822 |
| status job timestamp | 2026-06-03T20:04:38.875Z（pull 結束點、UTC） |
| 93/93 test PASS | ✅（pre-flight） |
| Lab session 過期 | 否（中途未刷新 token） |

**實際 vs spec 差異**：
- spec 預估 5-10 min、實際 43 min（×4.3 slower）
- 觀察：S3 累積速率前段 ~1-2 檔/min（download 主導、TDCS 端 throughput limit）、後段 ~10+ 檔/min（download 完進入併發 upload）
- 推斷主因：TDCS 端 download 是 sequential 或低併發、單檔 ~31 MB 在 home broadband 上慢；非 CLI bug、非 S3 bottleneck
- 建議：PLAN_E9 後續可考慮把 tdcs-download.ts 的 concurrency 從現在預設拉高（**屬優化、非本次 scope**）

**ai_workspace v1 對比**：
- v1 PLAN_E5：741 檔 / 22 GB / Python 腳本
- v2 本次：744 檔 / 23.39 GB / TS CLI 一鍵
- 檔數 +3（hourly 抓齊）、bytes ≈ 對齊（差異在 gzip level + 個別 file size）

### 後續行動
- **回 Lead 回報** → PLAN_E8 M6 驗收（M7 / M8 由 Lead 排程）
- **PLAN_E9 提示**：當前 status=`downloaded`、下一個 state 應是 `cleaning` → `cleaned` → `analyzed` → `done`（PLAN_E8 M3 6-state 預備）；S3 raw/ 目錄已 ready 給 cleaning step 消化
- **既知 deploy 觀察供 6 個月後接手**（§0.3 鐵則 1）：
  1. ink 進度條 stderr/stdout 在非 TTY 環境（tee / redirect）**不 flush**、要看進度必須直接 PowerShell 跑、別 redirect — 本次靠 `aws s3 ls` polling 補位、確認非 hang
  2. TDCS download 真實 throughput ~9 MB/s aggregate、單月 22 GB 要 ~40 min、不是 5-10 min（spec 估太樂觀、Lead 後續 PLAN 可校準）
  3. job_id 從 pull stdout 拿（不是從 API GW POST、本次未走 production Lambda、純 client-side flow）
- **無觸紅線**：未改 code、未裝額外 dep、未跑兩次（一次 OK）、token 未過期、無 5xx / 401 / 403
