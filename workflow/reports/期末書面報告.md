# TDCS 國道車流資料下載清洗 CLI ‧ 期末書面報告

> **第 17 組** ‧ 專題名稱：《17_關於我在無意間被隔壁車道的卡車學長撞成廢人這件事》
> 課程：[課程名稱、User 填]
> 交流道：國道 1 號麻豆段（01F2930N / 01F2930S / 01F3019N / 01F3019S）
> 組員：辛弈序（112021004）、謝秉宏（112021024）、陳睿綸（113021091）、謝晴如（114021168）
> 小組分工 / 貢獻度：謝晴如 — 文書製作（簡報 + 書面報告）25%；辛弈序、謝秉宏、陳睿綸 — 系統實作（均分）各 25%（暫填、繳交前可調）
> 繳交日期：2026 年 6 月 10 日
> Repo（教學版）：https://github.com/eason07-7/112021024TDCS

---

## 目錄

1. 系統定位
2. 系統架構
3. 概念設計八大重點（Concept Design Highlights）
   - 3.1 重複下載判斷 — 不浪費 S3 上傳
   - 3.2 SQS broker pattern — 30 秒 vs 8 分鐘的鴻溝
   - 3.3 F-H4 git SHA tag versioning — 不再用 :latest 鎖死
   - 3.4 F-H5 schema type drift — 抓 bug → root cause → 修好
   - 3.5 Lambda > Glue ETL trade-off — 對的工具解對的問題
   - 3.6 E6 client-side relay winner — 三跳板實驗的科學選擇
   - 3.7 Wizard 三選項輸出 — user 自選資料落點
   - 3.8 §0.3 四步妥協機制 — 永久債避免
4. 已驗證核心邏輯
5. 與 mcp Stage 1 manual 流程對比
6. v3.0 設計：自帶帳號、一次苦工、全民共享（含 cloud-portable + local-only 雙 fork）
7. 結語與致謝

---

## §1 系統定位

把 `mcp_workspace` 階段 1（2026/03 麻豆段 4 匝道 manual 流程）的「下載 → 清洗 → 視覺化 → AI 分析」四大管線，升級成**參數化、可重現、未來通用**的命令列工具（CLI）+ AWS backend。一句話：

> **同一個 CLI，麻豆段可以跑，未來任意路段也可以跑。**

階段 1 的 14,058 行清洗結果是手工跑 Python script 一次性產出、流程綁死月份 / 匝道 / 路徑。階段 2 的 v2 CLI 透過 wizard 收輸入、Lambda 真清洗、Athena 真可查、user 不需要動程式碼。

> **影片 demo**：5-8 分鐘示範 wizard → 下載 → 清洗 → S3 → Athena 全流程（YouTube 連結另於 Tronclass 提交、不列入本書面報告）。

---

## §2 系統架構

五件套：CLI（本機）+ Lambda（清洗）+ S3（儲存）+ Glue Data Catalog（schema）+ Athena（查詢）。

```
┌──────────────────────────────────────────────────────────────────────┐
│                          User 端                                       │
│  $ npx tdcs-dl     ← TUI wizard（5 步：資料 / 時段 / 匝道 / 輸出 / 確認）│
│        │                                                              │
│        ▼                                                              │
│  ┌─────────────────┐  ┌──────────────────────────────────────────┐  │
│  │ E6 client-side  │  │  HTTP API Gateway                          │  │
│  │ relay：本機直接  │  │  POST /pull   POST /clean   GET /jobs/<id> │  │
│  │ 抓 TDCS 上 S3   │  │                                            │  │
│  └─────────────────┘  └──────────────────────────────────────────┘  │
│        │                       │                                      │
└────────┼───────────────────────┼──────────────────────────────────────┘
         │                       │
         ▼                       ▼ (≤30s producer)
┌─────────────────────────────────────────────────────────────┐
│                        AWS us-east-1                          │
│  S3 (112021024):                                              │
│    raw/yyyymm=YYYYMM/*.csv.gz       ← E6 client-side 上傳     │
│    cleaned_v2/yyyymm=YYYYMM/cleaned.parquet                   │
│    jobs/<job_id>.json                ← JobRecord 6-state      │
│        ▲                                                       │
│        │                                                       │
│   SQS clean-jobs queue ──────► Lambda cleaner (Node.js 20)    │
│        │  (≤920s broker)        ├─ nodejs-polars 真清洗        │
│        ▼                        ├─ schema-mapping 9 欄         │
│   SQS DLQ (maxReceiveCount=2)   └─ write Parquet to S3        │
│                                                                │
│  Glue Data Catalog: database tdcs_dl / table cleaned_v2_skeleton │
│  Athena workgroup: tdcs-dl-wg (10 MB scan cap)                │
└─────────────────────────────────────────────────────────────┘
```

關鍵設計：

- **CLI ↔ AWS API 分離**：CLI 完全可離線運作（pull 走 E6 client-side 直接相對 TDCS）、AWS 只在 clean / Athena query 時必要
- **同步 producer + 非同步 consumer**：API GW 30 秒整合 timeout vs Lambda 最長 15 分鐘清洗時間、靠 SQS broker 切兩段，見 §3.2
- **Pascal→snake schema mapping**：TDCS 原 CSV header 是 PascalCase（`GantryID_O`），Glue + Parquet 統一 snake_case（`gantry_id_o`）、避免大小寫衝突

### §2.1 AWS 資源配置一覽（terraform spec ↔ AWS live 雙錨對齊）

下表每一列的「配置值」皆有兩個來源互相佐證：左為 Terraform 程式碼（configuration as code、可複現），右為 2026-06-04 從部署環境 `aws ... get-*` 實際撈回的 live 值——兩者完全一致，證明「寫的配置」與「跑的配置」零漂移。

| 資源 | 關鍵配置（live = terraform） | IaC 出處 | 配圖 |
|---|---|---|---|
| **Lambda** `tdcs-dl-cleaner` | memory **2048 MB**、timeout **900 s**、ephemeral **1024 MB**、package=Image(ECR)、trigger=API GW + SQS | `lambda.tf` + `sqs.tf` | demo_17 |
| **SQS** `tdcs-dl-clean-jobs` | visibility **920 s**（>Lambda 900 s）、DLQ redrive **maxReceiveCount=2**、retention 1 天 | `sqs.tf` | demo_18 |
| **SQS DLQ** `…-dlq` | retention **14 天**（失敗訊息保留供查） | `sqs.tf` | demo_18 |
| **Event source mapping** | SQS → Lambda、**batch_size=1**（單清洗任務/invoke） | `sqs.tf` | demo_17（trigger）|
| **Athena workgroup** `tdcs-dl-wg` | scan cap **10 MB**（10,485,760 B）、enforce=true、CloudWatch metrics on | `athena.tf` | demo_12 |
| **Glue table** `tdcs_dl.cleaned_v2_skeleton` | **9 欄 snake_case** + partition **`yyyymm`**、Parquet/SNAPPY | `glue.tf` | demo_19 |
| **API Gateway** `tdcs-dl-api`（HTTP API） | route **`POST /clean`** + **`GET /jobs/{id}`**、Lambda proxy 整合 | `apigw.tf` | demo_20 |
| **S3** `112021024` | prefix `raw/` · `cleaned_v2/` · `jobs/` · `athena-results/`（皆 `.gitkeep` marker 建立） | `s3.tf` | demo_21 |

> **工程意涵**：所有配置都進版控（Terraform），不是 Console 手點。`terraform apply` 即可在任意 AWS 帳號重建同一套環境——這是 §6 v3.0「自帶帳號、IaC 一鍵自架」的基礎。

---

## §3 概念設計八大重點

> 每點四段：**問題 / 觀察 / 對策 / 驗證**。引述均對應實驗紀錄事件 # + commit hash、可在 repo `workflow/logs/` 查證。

---

### 3.1 重複下載判斷 — 不浪費 S3 上傳

**問題**：

教學 demo bucket（`s3://112021024`）給全班共用，若 user A 下載 2026/05 01F3019 已上傳、user B 再選同月同匝道，現有設計會**重新從 TDCS 抓一次、再上 S3 一次**。22 GB / 月 / route 的 raw、跑十次就是 220 GB 浪費上傳頻寬 + Learner Lab S3 PUT 配額。

**觀察**：

`tdcs-dl pull` 本質上是 raw 資料的 fetch + persist、不是計算密集任務。S3 上若已存在同 `yyyymm` + `route` 的完整 raw set（檔數對得上 TDCS manifest），**本機根本不該再抓**、直接 reuse cloud 上的就好。

**對策**：

設計層加 **dedup gate**：
1. wizard step 4「選輸出」前、CLI 先呼叫 `HEAD s3://bucket/raw/yyyymm=YYYYMM/`、count gz 檔數
2. 若數量 ≥ TDCS manifest 預期數（744 for 31 天 × 24 hr）→ 提示 user「S3 已有完整 raw、要 reuse 還是強制重抓？」
3. user 選 reuse → CLI 跳過 pull、直接走 clean、省 ~30 分鐘 + 22 GB upload

**驗證**：

dedup gate 建立在**已驗證的能力**之上、不是憑空構想：raw 已依 `yyyymm` 分區存於 S3（§3.6 client-side 上傳鏈已實證 22 GiB / 744 檔落地），而「數同前綴下的物件數、比對 TDCS manifest 預期檔數」用的是 `ListObjectsV2` / `HEAD` 這類 S3 標準能力——判斷邏輯是確定可行的。它與 §6 v3.0 的「自帶帳號自架」「streaming pull」屬同一條設計主軸：**讓上游苦工只做一次、雲端共享，使用者不重複承擔**。

> 對應 commit / 事件：`infra/terraform/s3.tf`（raw partition 結構）+ §3.6 已驗證上傳鏈 / 設計詳見 §6 v3.0

---

### 3.4 F-H5 schema type drift — 抓 bug → root cause → 修好

> **本報告核心 narrative**：這條線完整展示了「**已驗證核心邏輯**的工程方法論」、從 bug 發現到 root cause 鎖定到驗證修好的三連 evidence chain。

**問題**：

PLAN_E9 端到端 smoke 跑完、Athena `SELECT COUNT(*) FROM cleaned_v2_skeleton` 拿到 14116 行對得上 baseline ✅。但跑 `SELECT vehicle_type, SUM(counts) GROUP BY vehicle_type` 立刻噴：

```
HIVE_BAD_DATA: Malformed Parquet file.
Field vehicle_type's type DOUBLE in parquet file ...
is incompatible with type integer defined in table schema
```

[配圖：`workflow/reports/screenshots/E10/demo_15_bug_schema_type_drift.png`]

**觀察**：

DOUBLE vs INT 型別不對齊。第一直覺：**SQL CAST 修**——`SELECT CAST(vehicle_type AS INTEGER) ...`。試了。**也噴同錯**：

[配圖：`workflow/reports/screenshots/E10/demo_15b_bug_cast_also_fails.png`]

關鍵觀察：**Athena CAST 救不了**。因為錯誤發生在「Parquet reader 讀檔的階段」、SQL CAST 是在「資料讀進 query engine 後」才能作用、根本到不了 CAST。問題在寫檔不在查詢。

**對策**：

回頭看 Lambda 清洗端的程式：

```typescript
// infra/lambda/src/index.ts（修前）
const df = pl.readRecords(parquetRows);
df.writeParquet(tmpParquet);
```

`pl.readRecords` 沒給 schema、polars 依推斷規則把 JS Number 通通推成 Float64（因為 JS Number 規範就是 64-bit float、polars 不知道我們意圖是整數）。Float64 → Parquet 寫成 DOUBLE → Glue table 對應欄是 INT → mismatch。

修法（PLAN_E10 對應 opus_worker_2 commit `a381a2d`）：**寫入前明示 schema**：

```typescript
const PARQUET_SCHEMA = {
  year: pl.Int32, month: pl.Int32, day: pl.Int32, weekday: pl.Int32,
  hour_0: pl.Int32, gantry_id_o: pl.Utf8,
  vehicle_type: pl.Int32, counts: pl.Int32, week_index: pl.Int32,
};
const df = pl.readRecords(parquetRows, { schema: PARQUET_SCHEMA });
```

**8 個 numeric 欄位 Int32 + 1 個字串欄 Utf8**、polars 不再猜、Parquet 就會落 INT32。

**驗證**：

修 + 重 deploy + 重 clean（覆蓋舊 DOUBLE Parquet）+ 重跑 Athena GROUP BY：

| Query | 結果 | Run time | Scanned |
|---|---|---|---|
| `SELECT vehicle_type, SUM(counts) ... GROUP BY vehicle_type` | **5 列 PASS、無 HIVE_BAD_DATA** | 767 ms | 32.67 KB |

[配圖：`workflow/reports/screenshots/E10/demo_16_athena_groupby_fixed.png`]

5 列結果：vehicle_type {5: 111,926 / 31: 980,484 / 32: 338,957 / 41: 10,767 / 42: 87,699}、總 1,529,833 次車輛通過。

**三連 evidence chain**：

`demo_15`（bug）→ `demo_15b`（CAST 也失敗、證明 root cause 不在 SQL）→ `demo_16`（顯式 schema 修好、Athena PASS）

**附帶效益 — Parquet 檔案縮 12 倍**：

修補的設計動機是正確性、但實際拿了一個**意外的效率紅利**。同一份清洗結果（仍是 14,116 行、同 9 欄）、僅 dtype 從 Float64/DOUBLE 改成 Int32：

| 版本 | Parquet 大小 | 行數 | dtype |
|---|---|---|---|
| 修前（demo_11）| **903.5 KB** | 14,116 | Float64 / DOUBLE |
| 修後（live 從 S3 撈）| **76,673 B**（74.9 KB）| 14,116 | Int32 |
| **縮減比** | **~12×** | — | — |

8 個 numeric 欄 Float64 是 8 bytes / value、Int32 是 4 bytes / value、加上 Parquet 對相同值的 dictionary encoding 在 Int32 上效果更好——同一份資料、寫入時多給 polars 一個 schema 提示，下游所有的儲存與掃描成本都連帶降。**明示優於推斷** 不只是正確性問題、也是效率問題。

**工程方法論收穫**：

1. **錯誤不在你以為的層**：CAST 不行是因為錯在 reader 不在 query。看完整 stack 而非只看 error message。
2. **明示優於推斷**：型別系統的「自動推斷」在跨層邊界（JS → Parquet）會出問題、跨層處要顯式宣告。同一份修正同時拿到「對」與「省」兩個結果。
3. **修完要重 deploy + 覆蓋舊資料**：S3 上舊 DOUBLE Parquet 不會自己變、要重清洗一次才有新 INT32 Parquet。

> 對應 commit / 事件：`commit a381a2d`（修）+ `commit 1a45d37`（重 deploy + 重 clean）/ 實驗紀錄 9 事件 #13+#14。Parquet 大小數據出處：demo_11 截圖（903.5 KB）+ 2026-06-04 從 S3 live 撈當前物件大小（76,673 B、可用 `aws s3 ls s3://112021024/cleaned_v2/yyyymm=202603/cleaned.parquet` 重現）。

---

### 3.2 SQS broker pattern — 30 秒 vs 14 分鐘的鴻溝

**問題**：

清洗業務透過 API Gateway HTTP API 觸發（`POST /clean`）。但 API Gateway 的 integration timeout 有一個**改不掉的服務硬上限：30 秒**。而我們單月清洗的實測時間是 **847 秒（14 分 7 秒）**——掃 744 個 raw 檔、聚合出 14,116 行。若 `POST /clean` 同步等清洗跑完才回應，API Gateway 會在第 30 秒直接回 504，**即使後端 Lambda 還在正常清洗**。30 秒 vs 847 秒，是 28 倍的鴻溝、不是調參數能補的。

**觀察**：

第一直覺是「把 Lambda timeout 調大」——但問題不在 Lambda（它本來就設 900 秒），在 API Gateway 那 30 秒。第二直覺是「換一個沒有 30 秒限制的觸發方式」——這才對：關鍵 insight 是**把「觸發」與「執行」拆開**。使用者要的其實不是「站在原地等 14 分鐘」，而是「丟出任務、拿到一個追蹤碼、之後查進度」。這正是經典的 **producer–consumer 非同步模式**：producer 收到請求**立刻**回一個 job_id，consumer 在背景慢慢做。

**對策**：

導入 **SQS 佇列**當 broker，同一個 Lambda 掛兩個入口（dual dispatch）：

```typescript
export async function handler(event) {
  if (isSqsEvent(event)) return handleSqsBatch(event);   // consumer：真清洗 5-14 min
  return handleApiGwRequest(event);                       // producer：秒回 202
}
```

- **producer 路徑**（API GW）：寫 `status=accepted` → `SendMessage` 進 SQS → 立刻回 `202 {job_id}`（< 1 秒）
- **consumer 路徑**（SQS event source）：背景跑 list→clean→Parquet→Athena REPAIR → 寫 `status=done`
- 配置上用 SQS 標配護網（非自造）：`visibility_timeout=920s`（> Lambda 900s、避免任務還在跑就被重投）、DLQ `maxReceiveCount=2`（失敗重試 1 次後進死信佇列、不無限重跑）、`batch_size=1`（一個清洗任務一個 invoke）。

**驗證**：

實跑 202603：`POST /clean` 即時回 `202 + job_id`（< 1 秒、非 504）；CLI `pollUntilDone` 每 5 秒查一次 `GET /jobs/{id}`，狀態 `accepted → processing → done`；總耗時 847 秒、14,116 行落地。Lambda Console 的 Function overview 同時掛著 **API Gateway + SQS 兩個 trigger**，即 dual dispatch 的實證。

> 對應 commit / 事件：`infra/terraform/sqs.tf` + `infra/lambda/src/index.ts`（isSqsEvent dual dispatch）/ 實驗紀錄 9 事件 #9（M4.5 SQS broker 設計與 25 test）+ 事件 #11（M6 端到端首次實戰）
> [配圖：demo_17_lambda_general.png（Lambda 雙 trigger）+ demo_18_sqs_settings.png（佇列 + DLQ）+ demo_08_stage2_cleaning.png（CLI 輪詢中）]

---

### 3.3 F-H4 git SHA tag versioning — 不再用 :latest 鎖死

**問題**：

Lambda 以 container image 部署（image 放 ECR）。最初 `image_uri` 綁 `:latest` tag。流程是：改 handler → build → push 新 image 到 `:latest` → `terraform apply`。但 `image_uri` 字串（`…:latest`）**沒變**，Terraform 的宣告式 diff 判定「無變更」、不更新 Lambda——於是 Lambda **繼續跑上一版 image**。結果就是「我明明改了程式、重部署了，Lambda 還跑舊 code」的鬼打牆，debug 時極易誤判成「清洗邏輯有 bug」（其實是 image 沒換）。

**觀察**：

`:latest` 是一個 **mutable pointer**：同一個字串、指向的內容會變，而 Terraform 只看「字串變了沒」。第一直覺是 `terraform apply -replace=…` 強制重建——但那要求部署者**每次都記得帶這個 flag**，忘了跑純 `apply` 就又靜默回舊 image，等於把正確性託付給人的記憶。關鍵 insight：**tag 本身就該是 immutable、而且與 git commit 1:1 對應**——讓「字串」隨每次部署改變，Terraform 的宣告式模型自然就偵測得到。

**對策**：

`build_and_push.sh` 改用 git commit SHA 當 tag（working tree 有未提交變更時補時間戳保唯一），寫進 Terraform 自動載入的 `image_tag.auto.tfvars`：

```bash
TAG="$(git rev-parse --short=12 HEAD)"          # e.g. a381a2d7a88a
[ -n "$(git status --porcelain)" ] && TAG="${TAG}-dirty-$(date +%Y%m%d%H%M%S)"
echo "lambda_image_tag = \"${TAG}\"" > ../terraform/image_tag.auto.tfvars
```

於是 `image_uri` 字串隨每次部署改變 → `terraform plan` 偵測到 → **in-place 更新** Lambda code。部署者只跑 `terraform apply`、不必記任何 magic flag。

**驗證**：

兩次真實部署實證機制有效：M6 首次部署 tag = `b71afeba8f70-dirty-…`；F-H5 修補第二次部署 tag = `a381a2d7a88a-dirty-20260604093928`，`terraform plan` 顯示 **0 added / 1 changed / 0 destroyed**（純 `image_uri` in-place、無 destroy/recreate），Lambda 約 10 秒更新完。注意 tag 前綴 `a381a2d` **正是 F-H5 那個 git commit 的 SHA**——部署在跑的 image 與原始碼版本 1:1 可追溯，這正是 `:latest` 永遠做不到的。

> 對應 commit / 事件：`infra/lambda/build_and_push.sh` + `infra/terraform/image_tag.auto.tfvars` / 實驗紀錄 9 事件 #1（F-H4 gate 批 A 設計、含 trade-off vs `-replace`）+ 事件 #11（M6 首次部署 b71afeba8f70）+ 事件 #14（F-H5 第二次部署 a381a2d7a88a、plan 0/1/0）

---

### 3.5 Lambda > Glue ETL trade-off — 對的工具解對的問題

**問題**：

清洗引擎選型。AWS 上兩個方向：**AWS Glue ETL**（Spark、企業級批次、課程與教科書的「大數據招牌」）vs **Lambda**（輕量、短任務）。直覺上「大數據」就該配 Spark。但問題該問的是：用一個 Spark cluster 去殺我們這個量級的資料，是合理、還是 overkill？我們的單月輸入是 **~22 GiB raw（744 個 csv.gz）**，清洗後聚合成 **14,116 行**。

**觀察**：

看清楚 transform 的形狀才知道該用哪個工具。我們的清洗是：逐檔 gunzip → 篩目標 gantry → 依（時 × 門架 × 車種）groupby 計數 → 加週次索引。**全程沒有跨檔 join、沒有 shuffle、沒有跨節點協調**。Spark 的價值正在於 join/shuffle/跨節點的分散式運算——這裡一個都用不到。關鍵 insight：這是 **Lambda 的 sweet spot**（短任務 < 15 min、簡單 transform、零叢集管理），不是 Glue 的；硬上 Spark，叢集初始化與排程的固定開銷反而可能比清洗本身還久。

**對策**：

鎖板「**純 Lambda 處理層**」（MASTER_PLAN §0 M4.2）：單一 TS 清洗模組（< 300 行）、`nodejs-polars`、Lambda 2048 MB（≈ 1.1 vCPU）、streaming 一次處理一個檔控制記憶體峰值。不引入 Glue / Step Functions。報告誠實寫「為什麼**不**選 Glue」——這是工程判斷、不是能力不足。

**驗證**：

實測單月清洗 **847 秒（14 分 7 秒）**、輸出 14,116 行，落在 Lambda 900 秒 timeout 內。值得一提的是「知道怎麼更快、但有意識地不做」：Lambda 的 CPU 隨記憶體線性配（調到 10240 MB ≈ 6 vCPU 會更快），但瓶頸還包含 22 GiB 的 S3 下載 I/O + 逐檔單線程處理，加記憶體報酬遞減；單月 14 分鐘已在配額內，為省幾分鐘而上平行/Step Functions 架構反而是過度工程。這正是 §0.3「工程合理性——不過度」的具體落實：對的工具解對的問題。

> 對應 commit / 事件：MASTER_PLAN §0 鎖板表 M4.2（純 Lambda、反 Glue Spark 的工程選擇）+ 實驗紀錄 9 事件 #11（M6 端到端 817s）+ 事件 #14（重清 847s / 14,116 行）

---

### 3.6 E6 client-side relay winner — 七種跳板的科學選擇

**問題**：

TDCS 公開資料（`tisvcloud.freeway.gov.tw`）**只接受台灣 IP**，而我們的 AWS backend 鎖死 us-east-1（Learner Lab 強制）。「怎麼讓 us-east-1 抓到只給台灣 IP 的資料」是整個架構最大的洞——它決定產品能不能 cloud-native。

**觀察**：

不靠拍腦袋，設計 **7 個對照實驗（E1–E7）× 4 指標（latency / cost / reliability / deploy 複雜度）**。先用 E1（Lambda 直連）+ E2（EC2 直連）建立基線：兩者打 TDCS 都是 **silent drop**——E1 timeout 15,990 ms、E2 `curl exit 28` / `http_status 000`（連 TCP 握手都沒完成）。**實證封鎖是 region-level**（us-east-1 整段 IP），換 NAT/服務類型都沒用。所以一定要繞。候選方案各有代價：E3 第三方 proxy（$4–16/月 + 信用卡 + 爬蟲條款風險）、E4 Tailscale（3 個 SPOF + $0.09/GB egress + ~55 min setup）、E5 nginx + Cloudflare tunnel（免費但 user 機 SPOF + quick tunnel URL 不穩）、E7 hybrid webhook（5 個依賴鏈）。關鍵 insight：**下載必須從台灣 IP 出去，是物理約束、不是設計選擇**。

**對策**：

選 **E6 純 client-side**（MASTER_PLAN §0 M4.1 鎖板）：使用者本機（台灣 IP）直連 TDCS 下載 → gzip → 上傳 S3；AWS 只負責「收檔 + 清洗 + 查詢」這些真正的重活、不負責去「拉」（也拉不到）。這同時化解了「E6 把下載放本機是否違反『AWS 做重活』」的疑慮——重新框定後答案很清楚：**下載是台灣 IP 物理約束、清洗與查詢才是重活，而那兩者都在雲端**。

**驗證**：

PoC 已通過（`backfill_s3_2026.py` 走完整 download → gzip → S3 鏈）；PLAN_E8 production 真跑麻豆段 **22 GiB / 744 檔 / 43 分鐘 / 6-state JobRecord**、零跳板費用。E1/E2 的 timeout 數據就是「為什麼非繞不可」的硬證據——這是把一個架構決策建立在實測對照上、而非假設。

> **設計延伸（streaming pull、概念設計）**：目前 pull 是「整月下載到本機 temp → 全數上傳 → 刪除」，峰值本地佔用 ~22 GiB（任務結束後歸零）。下一步設計是 **streaming pull**——邊下載、邊上傳、邊刪除（HTTP → gzip → S3 串流、不落地），讓本機峰值佔用恆定在 ~1 個檔、且中斷不留殘留。這把「使用者電腦不需承擔大資料存儲」從「長期歸零」推進到「過程中也趨零」，與清洗端 Lambda 既有的「一次一檔 streaming」設計一致（同一套資源恆定哲學、客戶端與伺服端對齊）。

> 對應 commit / 事件：`workflow/reports/brief_relay_experiments_2026-06-02.md`（E1–E7 七實驗 4 指標）+ MASTER_PLAN §0 M4.1（E6 winner 鎖板）+ 實驗紀錄 6/7/8（CLI → AWS infra → download chain 演進）

---

### 3.7 Wizard 輸出落點 — 重活在雲、結果由 user 自選

**問題**：

CLI 跑完，資料該放哪？這背後其實是更根本的問題：**使用者的電腦該承擔多少？** 一個月 raw 是 ~22 GiB、清洗要十幾分鐘的 CPU；若把「存 22 GiB」和「跑清洗」都壓在使用者本機，等於要求每個研究者的筆電都先有大硬碟 + 長時間燒 CPU——這正是大多數人卡在「前置苦工」的原因。

**觀察**：

關鍵是把「**重活的落點**」和「**結果的落點**」分開：
- **重活**（22 GiB raw 存儲、清洗 compute）→ 必須在雲端。本機扛不起、也不該扛。
- **結果**（清洗後的輕量 Parquet，單月僅 ~76 KB）→ 才讓使用者選要不要拉回本機。

於是 wizard 第 4 步「輸出位置」提供三選項：**本機 / S3 only / 兩邊**，對應不同工作流（demo 只要雲端、本機開發要落地、混合則兩邊）。

**對策**：

架構上「重活恆在雲」是已落地的事實：raw 上 S3、清洗在 Lambda、本機下載用的 temp 上傳完即刪（本機**長期零存儲**）。輸出落點則由使用者於 wizard 自選（`cli/src/wizard/state.ts` 的 `OutputDest = 'local' | 's3' | 'both'`、`Output.tsx` radio 選單），不寫死、不強迫兩邊都存。

**驗證**：

核心價值「**雲端扛存儲 + 清洗、本機保持輕量**」已端到端驗證：單月 22 GiB raw 存於 `s3://112021024/raw/`、清洗在 Lambda 跑 847 秒、本機 temp 清空、結果 `cleaned.parquet` 落於 `s3://…/cleaned_v2/yyyymm=…/`（Athena 查得到 14,116 行）。三選項中 **S3 輸出已驗證**；**「本機儲存 → `./tdcs-output/`」目前為設計（wizard 已具該選項與 UI），將清洗結果下載回本機的實作列為後續完善**——本報告誠實標示其實作邊界，不宣稱本機已落檔。

> 對應 commit / 事件：`cli/src/wizard/state.ts`（`OutputDest`）+ `cli/src/wizard/steps/Output.tsx`（三選項 UI）+ 實驗紀錄 8（wizard 五步）+ 實驗紀錄 10 事件 #3
> [配圖：demo_05_step_output.png（三選項 UI）]

---

### 3.8 §0.3 四步妥協機制 — 不讓「趕時間」變成永久債

**問題**：

工程不可能永遠 100% 完美，總會撞上「時間壓力 vs 品質」的取捨。PLAN_E9 的 M6 驗收原訂一條硬指標：清洗結果要對 Stage 1 的 **14,058 行 baseline 做 byte-level md5 對齊**。但實際清洗 production raw 跑出 **14,116 行**——多 58 行，md5 必然不一致。距 demo 只剩 6 天，要不要「先湊個過」？

**觀察**：

先查根因、再決定怎麼處理。+58 行**不是 bug**：v2 的 raw 抓齊了 744 個 hourly 檔，而 v1 當年的 baseline 只有 741 檔（漏抓 3 個小時），3 小時 × ~20 行 ≈ +58，數字對得上。關鍵 insight：既然輸入本來就不同，「對 14,058 行做 byte-md5」這個**指標本身就不合理**——拿不同輸入去比 md5，永遠不會一致；硬湊就是自欺。

**對策**：

不降品質、改用更誠實的驗證——**structural 三證**取代 byte-md5：① **sync copy axiom**（`cli/` 與 `infra/lambda/` 兩份 `tdcs-clean.ts` byte diff = 0、邏輯同源）；② **row count**（14,116、+58 已解釋）；③ **Athena schema**（9 欄 snake_case + INT32 對齊、F-H5 後）。byte-md5 不丟棄，延到 PLAN_E11 對 production raw 重跑 reference 做實證 gate。

**驗證**：

這不是「先 hack 跑通再說」，而是**完整走過** `workflow/CLAUDE.md §0.3` 的「四步妥協補救流程」：① 妥協明文寫進實驗紀錄 9 事件 #12 的「妥協紀錄」段（為什麼妥協 / 妥協了什麼 / 何時補）；② PLAN_E9「後續行動」追加未來修補項；③ MASTER_PLAN §7 風險表新增「sync copy lib drift」一條；④ 下個 PLAN（E11）啟動前由 Lead 主動 review 此 pending + 加 byte-md5 gate。走完這四步的妥協，是**有紀律的技術決策**而非永久債——這本身就是工程方法論的展示。

> 對應 commit / 事件：實驗紀錄 9 事件 #12（M6 整合 + §0.3 四步補救）+ `workflow/CLAUDE.md §0.3` + MASTER_PLAN §7 風險表

---

## §4 已驗證核心邏輯

> 本節用「真實數據 + 截圖證據」證明：CLI 不是 demo-only、是真清洗、真可查、真可重現。

### 4.1 端到端跑通數據

| 階段 | 輸入 | 輸出 | 證據 |
|---|---|---|---|
| pull | TDCS 2026/03 麻豆段 4 匝道 | 744 raw csv.gz / 22 GiB | demo_07 + S3 console |
| clean | 744 raw | cleaned.parquet / 14,116 rows / **75 KB**（INT32、F-H5 後）| live `aws s3 ls`（76,673 B）+ demo_11 |
| Athena COUNT(*) | cleaned.parquet | 14,116 | demo_13 |
| Athena GROUP BY vehicle_type | cleaned.parquet | 5 列 / 1,529,833 通過數 | demo_16 |

[配圖：demo_11_aws_console_s3.png + demo_13_athena_query_count.png + demo_16_athena_groupby_fixed.png]

> **數據註（2026-06-04 live 復核）**：上表數值皆於 demo 後直接以 `aws ... get-*` / Athena live 查詢復核——`COUNT(*)`=14,116、`GROUP BY` 5 列共 1,529,833（重查 scan 29.8 KB / 778 ms）。`cleaned.parquet` 現值 **76,673 B（75 KB、INT32）**；demo_11 截於 **F-H5 修補前**、顯示 DOUBLE schema 版本 **903.5 KB**——同樣 14,116 行、僅型別不同，INT32 + Parquet 對整數的字典/RLE 編碼壓縮遠優於 DOUBLE，使檔案縮約 12×。這是 F-H5（§3.4）schema 修正的附帶效益。

### 4.2 Structural 三證 baseline

對齊原 mcp Stage 1 baseline 14,058 行的「實質正確性驗證」分三層：

1. **Sync copy axiom**：CLI 端 `tdcs-clean.ts` 與 Lambda 端 `index.ts` 共用 header byte-level diff = 0
2. **Row count**：14,116 行（含 3 個 hourly correction、與 14,058 source-of-record 差異 +58 / +0.41%）
3. **Athena schema**：9 欄 snake_case 對齊 Glue table、type INT32 / Utf8 對齊（F-H5 修後）

> 對應實驗紀錄 9 事件 #11 + #12 + MASTER_PLAN §7 風險表

---

## §5 與 mcp Stage 1 manual 流程對比

> 階段 1（2026 年 4-5 月）vs 階段 2（2026 年 5-6 月）量化對比、體現工程價值。

| 維度 | Stage 1 manual | Stage 2 v2 CLI | 提升 |
|---|---|---|---|
| 重現性 | Python script + 硬編 path、跨機器重跑要改 5+ 處 | `npx tdcs-dl` 一鍵、wizard 五步 | 無腦可重現 |
| 通用性 | 綁死 2026/03 + 麻豆 4 匝道 | wizard 收年月 + 任意匝道 list（339 個 gantry） | 從 1 case → ∞ cases |
| user 介入 | 用 Python、要懂 venv / argparse / pandas | npm CLI、不需要 Python 知識 | 對 non-coder 友善 |
| 清洗時間 | 手 trigger Python、~10 min 但要 user 監看 | wizard 觸發 / Lambda 自跑、user 不必盯 | 釋放 user 時間 |
| 證據鏈 | Python output csv、無歷史 | Athena query 可重複、S3 永久存 Parquet、Glue catalog | 可審計 |
| 工程文檔 | scratch notes | 7 PLAN + 14 實驗紀錄 + MASTER_PLAN + workflow CLAUDE.md | 可傳承 |

> 工程價值的本質不在「跑得快」（清洗時間差不多）、在「**可重現 + 可通用 + 可審計 + 可傳承**」。

---

## §6 v3.0 設計：自帶帳號、一次苦工、全民共享

本報告示範的是 **demo / 教學版**——用 AWS Learner Lab 展示工程方法論，有兩個本質侷限：(1) Lab 帳號 4 小時 session、學期結束收回；(2) 全班共用同一個 bucket + 共用憑證，既不安全也無法 scale。v3.0 的設計主軸就是把這兩個侷限拆掉，而**核心不變**：同一份 wizard、同一份清洗邏輯、雲端依舊承擔大資料的存儲與清洗、使用者的電腦保持輕量。以下五項設計，是 v2 已驗證核心之上的自然演進。

**1. 自帶 AWS 帳號 + IaC 一鍵自架（取代共用金鑰）**

交付物是 **CLI + Terraform IaC**，不是一把要分享的鑰匙。使用者拿**自己的** AWS 帳號跑一次 `terraform apply`，就有**自己的** S3 / Lambda / Athena / SQS 後端，用**自己的**金鑰：

```
$ git clone <repo> && cd infra/terraform
$ terraform apply          # 換成你自己的 account → 你自己的後端
$ tdcs-dl config set-endpoint <你自己的 API GW URL>
```

誰都不必跟我們拿金鑰，彼此的額度、資料、安全邊界完全隔離——這同時解掉教學版的「共用 bucket」與「公開 endpoint 濫用（F-H3）」兩個問題。配置全在 Terraform（§2.1），複現只是一個指令。

**2. 重複下載判斷（dedup）**——見 §3.1。S3 已有完整 raw set 就跳過 TDCS 下載 + 上傳，省 ~30 分鐘 + 22 GiB 流量。「上游苦工只做一次、雲端共享」的關鍵節能。

**3. streaming pull**——見 §3.6。邊下載、邊上傳、邊刪除，本機峰值佔用恆定在 ~1 個檔、且中斷不留殘留；與清洗端 Lambda 的「一次一檔」streaming 一致。

**4. presigned URL 免金鑰上傳大檔**——上傳 22 GiB 不該走 API Gateway（10 MB payload 上限）。由一個端點發短效、限定路徑的 **S3 presigned PUT URL**，client 拿它直傳 S3（繞過 API GW 限制），既不持有長期金鑰、又能搬大檔。這是「clean 不需呼叫端金鑰」（§3.2）對應到「上傳」側的乾淨解。

**5. 彈性時間區間**——目前 wizard 限 ≤ 3 個月 preset（F-H1 為防單一 Lambda invoke timeout 而設）。v3.0 後端改「**逐月 invoke**」（每月一個 SQS 訊息 / 一個 Lambda invoke、天然 chunk、永不撞 15 分鐘上限）後，前端即可放寬成任意起訖月份。

**6. Backend portability — 同一份 CLI、兩條 fork**

上述五項是 cloud-native fork（v3.0a：自帶 AWS 帳號、保留同一套 S3/Lambda/Athena 架構）。但 v3.0 還有一條同等正當的 fork：**純本地版**（v3.0b）——把 Athena 換成 **DuckDB**（本機就能跑 SQL 查 Parquet）、把 Lambda 清洗收回本機 worker process、把 S3 收回本地 filesystem。**wizard 五步流程不變、清洗邏輯（`tdcs-clean.ts`）不變、輸出 schema 不變**——換的只是後端執行引擎。

| fork | 後端 | 適合情境 |
|---|---|---|
| v3.0a cloud-portable | 自己的 AWS 帳號（同 v2 架構） | 多人共享資料湖、團隊長期維護、可 audit |
| v3.0b local-only | DuckDB + 本地 Parquet | 個人研究、無雲端帳號、隱私敏感資料 |

兩條 fork 的設計本身就是工程價值的展示：**核心邏輯與執行平台解耦**——使用者依場景選 backend、不必為了換引擎重寫 client。這也是 v2 「Pascal→snake schema mapping + sync copy 公理」（§3.4 + §3.8 提及）所鋪的伏筆——schema 與清洗邏輯一旦穩定，誰來執行其實是 swap 的問題。

> 這六項背後是同一句話：**讓大資料的存儲與清洗壓力**留在最合適的地方執行**、一次做好給所有人共享，使用者的電腦只發任務、拿輕量結果。** v2 已證明這套設計在雲端跑得起來（§4 真數據）；v3.0 把它從「一個共用教學帳號」推進到「人人可自架的事實標準前置工具」。demo 後將開公開 GitHub repo（與教學 repo 區隔）長期維護。

---

## §7 結語與致謝

[Lead 寫骨架、User 與老師 / 助教關係定後補]

### 工程方法論收穫

1. **抓 bug 看完整 stack**（F-H5 教訓：CAST 救不了是因為錯不在 SQL）
2. **明示優於推斷**（F-H5 schema 修法：跨層邊界顯式宣告 type）
3. **對的工具解對的問題**（§3.5 Lambda > Glue ETL）
4. **時間壓力下縮 scope、不降品質**（§3.8 §0.3 4 步妥協機制）
5. **工程價值是可重現 + 可審計 + 可傳承**（§5 對比）

### 致謝

[User 補：指導老師 / 助教 / 同學 / AI 協作工具（Claude Opus 4.7 1M context 作為 Lead 駕駛、AI Studio HTML 動畫簡報、opus_worker / deploy_worker / sonnet_worker 分工協作 6 名 worker）]

---

## 附錄 A：repo 結構

```
mcp_workspace/
├── cli/                    # TypeScript CLI（ink TUI + commander）
│   ├── src/wizard/         # 5-step TUI
│   ├── src/commands/       # pull / clean / status
│   └── src/lib/            # tdcs-clean.ts（sync copy 起源）
├── infra/
│   ├── lambda/             # Lambda container source（Node.js 20 + polars）
│   └── terraform/          # IaC（API GW + Lambda + S3 + Glue + Athena + SQS）
├── workflow/               # PLAN + 實驗紀錄 + MASTER_PLAN + reports
└── step0_s3_download/      # Stage 1 既有產物（保留作對比）
```

## 附錄 B：實驗紀錄 + 報告對應索引

| 報告章節 | 主要實驗紀錄 |
|---|---|
| §3.1 dedup 判斷 | 概念設計（建立於 §3.6 已驗證上傳鏈 + `infra/terraform/s3.tf` raw 分區）|
| §3.2 SQS broker | 實驗紀錄 9 事件 #9（M4.5）+ #11（M6 首次實戰）|
| §3.3 F-H4 SHA tag | 實驗紀錄 9 事件 #1（gate 批 A 設計）+ #11 + #14（兩次部署實證）|
| §3.4 F-H5 schema | 實驗紀錄 9 事件 #13（修）+ #14（重 deploy 驗）|
| §3.5 Lambda vs Glue | MASTER_PLAN §0 鎖板表 M4.2 + 實驗紀錄 9 事件 #11 + #14 |
| §3.6 E6 client-side | brief_relay_experiments_2026-06-02.md + 實驗紀錄 6/7/8 |
| §3.7 Wizard 輸出 | `cli/src/wizard/state.ts` + 實驗紀錄 8 + 實驗紀錄 10 事件 #3 |
| §3.8 §0.3 四步妥協 | 實驗紀錄 9 事件 #12 |
| §4 已驗證核心邏輯 | 實驗紀錄 9 事件 #11 + #14（F-H5 deploy）+ 實驗紀錄 10 事件 #3（live 復核）|
| §5 manual 對比 | step0_s3_download/ ~ step4_ppt/ 既有產物 |

---

> **本報告為 markdown 源、PDF 由 Pandoc xelatex 轉換**（PLAN_E10 M3）
> 字型：Microsoft JhengHei（中）+ Times New Roman（英）
> 對應 commit：[M3 完成後填 PDF commit hash]
