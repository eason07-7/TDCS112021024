# 實驗紀錄 10（PLAN_E10：期末三件套 — 書面 PDF + PPT + YouTube demo 影片）

> **PLAN_E10 標題校正紀錄**（2026-06-04 Lead pivot）：
> PLAN_E9 close 時 close_plan 從 MASTER_PLAN §7 抄的標題是「Athena subcommand + UX 打磨 + npm publish」（MASTER_PLAN 原規劃 E10-E13 線性走）。
> User 於 PLAN_E9 期間拍板「期末後砍 AWS、純本地 npm v3.0」+ demo 6/10 在即、Athena subcommand / npm publish 都會在 post-demo pivot 中 reset、繼續走原 E10 = 浪費功 + 拖累 demo。
> 故 PLAN_E10 pivot 為**期末三件套交付**（原 MASTER_PLAN §7 E13 內容）、原 E10/E11/E12 標為 "demo 後重新評估、可能整段砍"（MASTER_PLAN §7 加 pivot 註）。

> 舊紀錄封存於 `logs/實驗紀錄9.md`
> 本紀錄對應 `PLAN_E10.md`

---

> 歷史紀錄總目錄在 `INDEX.md`(自動產生)、本紀錄不寫 Compact 表。

## 當前狀態

| 項目 | 內容 |
|---|---|
| PLAN_E10 啟動 | 2026-06-04（Lead 寫完 spec） |
| Demo deadline | 2026-06-10（課程展示） |
| YouTube 上傳 deadline | 2026-06-09 |
| 三件套交付項 | 書面 PDF + PPT（AI Studio HTML 動畫）+ YouTube 影片（5-8 min） |
| Narrative 核心 | 已驗證核心邏輯 + 工程方法論（抓 bug → root cause → 修好）、不講 AWS infra 多炫 |

## Phase 進度(PLAN_E10 內)

| Milestone | 狀態 | 派工 |
|---|---|---|
| M0 spec typo 對齊 | ✅ | Lead 自做（grep verify-only、active 檔零 hit） |
| M1 demo_08/09/12 截圖補拍 | ⏳ | [USER_RUN] |
| M2.1 書面骨架 | ✅ | Lead 自寫（含 §3.1/§3.4 範本兩點） |
| M2.2 8 highlights 長文 | ✅ | opus_worker_2（b3ff221、22404 字元） |
| M2.3 Lead 審稿 | ✅ | Lead 自做（事件 #4、補 §3.4 縮檔 + §6 portability + TOC sync） |
| M3 書面 → PDF | ⏳ | [USER_RUN] Pandoc xelatex |
| M4 Presentation brief | ⏳ | Lead 自寫 |
| M5 PPT (AI Studio HTML) | ⏳ | [USER_RUN] |
| M6 影片 narration script | ⏳ | Lead 自寫 |
| M7 錄影 + YouTube 上傳 | ⏳ | [USER_RUN] |

---

## 事件紀錄區

(新事件追加在此區段的末尾、v0.6 §4.1 規範)

---

## [2026-06-04 21:30] 事件 #1 [Lead] [Phase 2] — M0 spec typo 對齊 verify-only 收尾

### 背景

deploy_worker 1a45d37 回報觀察 #1+#2：Lead 寫 spec 時把 `cleaned_v2_skeleton`（Glue table 實名）寫成 `cleaned_v2`、把 `tdcs-dl-wg`（Athena workgroup 實名）寫成 `tdcs_dl_wg`。雖然不擋 deploy_worker（他自己看了 terraform get-tables 修正）但會擋未來 worker 與 brief reader、Lead 拍板要 grep 全域一次性對齊。

### 目標

active 工作流 + 文件全域 grep `cleaned_v2[^_a-z/]` 與 `tdcs_dl_wg`、把 typo 修到正確名（`cleaned_v2_skeleton` / `tdcs-dl-wg`）、logs/ 封存歷史不動（per workflow §8）。

### 處理步驟

1. `grep -rn "cleaned_v2[^_a-z/]\|tdcs_dl_wg" workflow/ **/*.md` 全域掃
2. 結果 6 個檔有 hit：`workflow/PLAN_E10.md` + `workflow/MASTER_PLAN.md` + 4 個 `logs/`
3. 逐檔審：
   - `PLAN_E10.md` M0 段：是描述「要改什麼」、自身正確
   - `MASTER_PLAN.md`：line 62/137/243/263 都是 `cleaned_v2/`（S3 prefix 路徑、**不是 Glue table 名**）、本來就正確；line 318 是 PLAN_E10 M0 描述、正確
   - `logs/PLAN_E9_done.md` / `logs/實驗紀錄9.md` / `logs/PLAN_E7_done.md` / `logs/實驗紀錄7.md`：封存歷史、**不動**（per workflow §8）
4. 補掃 `workflow/reports/` 兩個 brief + 5 個 `README.md`：0 hit
5. 對照 `infra/terraform/athena.tf` line 9 + `glue.tf` line 13：實名確認 `tdcs-dl-wg` / `cleaned_v2_skeleton`

### 結果與數據

| 檔案類別 | hit | 動作 |
|---|---|---|
| `workflow/PLAN_E10.md` | 3（自描述） | 不動、正確 |
| `workflow/MASTER_PLAN.md` | 5（全 S3 prefix） | 不動、正確 |
| `workflow/reports/*.md` | 2（全 S3 prefix） | 不動、正確 |
| `**/README.md` | 0 | 無 |
| `logs/*.md` | 27+ | 不動、封存歷史 |
| **active 檔 typo 需修** | **0** | **無改動** |

terraform 實名 ground truth：
- `athena.tf:9` `name = "tdcs-dl-wg"` ✅
- `glue.tf:13` `name = "cleaned_v2_skeleton"` ✅
- `glue.tf:8` `database = "tdcs_dl"`（注意：query 寫 `tdcs_dl.cleaned_v2_skeleton`、不是 `tdcs_dl.cleaned_v2`）

### 後續行動

- M0 closed、verify-only、無 commit needed
- M1 截圖 + M2.2 8 highlights 併入 opus_worker onboarding（next step）
- M2.1 書面骨架 Lead 平行寫（next step）
- opus_worker 寫 highlights 時引述表 / workgroup 名要照 ground truth（onboarding prompt 內會明示）

---

## [2026-06-04 22:00] 事件 #2 [Lead] [Phase 2] — M2.1 書面報告骨架完

### 背景

PLAN_E10 M2.1 任務、書面報告 .md 骨架先由 Lead 寫、剩餘長文段（§3 八點中的 6 點）派 opus_worker 跟進。Lead 寫範本 1-2 點當 opus_worker 的撰寫樣板、避免 worker 套錯格式。

### 目標

產出 `workflow/reports/期末書面報告.md` 含：
- 完整大綱（§1-§7 + 附錄 A/B）
- §3.1 重複下載判斷 — Lead 全寫範本（概念設計型 highlight）
- §3.4 F-H5 schema type drift — Lead 全寫範本（技術 bug 型 highlight、含 demo_15/15b/16 三連 evidence chain）
- §3.2/3.3/3.5/3.6/3.7/3.8 — opus_worker 撰寫要點 hint placeholder

### 處理步驟

1. 讀 PLAN_E10 §「Concept design highlights」8 點清單
2. 讀 MASTER_PLAN §0 鎖板表（Lambda > Glue 的 narrative）+ §7 風險表（§0.3 4 步妥協範例）
3. 讀 實驗紀錄9 事件 #6 (SQS broker) + #11 (M6 smoke) + #12 (§0.3 4 步妥協 pivot) + #13 (F-H5 修) + #14 (重 deploy)
4. 寫骨架：封面 + 目錄 + §1 系統定位 + §2 系統架構（ASCII diagram）+ §3 八點 + §4 已驗證核心邏輯 + §5 對比表 + §6 v3.0 預告 + §7 結語 + 附錄 A/B
5. §3.1 全寫範本（dedup waste、user 明示重要）
6. §3.4 全寫範本（F-H5、本報告核心 narrative）
7. §3.2/3.3/3.5-3.8 — placeholder 留「問題 hook + 撰寫要點 + 引述出處」三段提示

### 結果與數據

| 區段 | 字數估 | 狀態 |
|---|---|---|
| §1 系統定位 | ~150 字 | ✅ Lead 寫完 |
| §2 系統架構 | ~400 字 + ASCII diagram | ✅ Lead 寫完 |
| §3.1 dedup waste | ~600 字 | ✅ Lead 全寫範本 |
| §3.4 F-H5 | ~900 字 + 3 截圖配 + 工程方法論 3 條 | ✅ Lead 全寫範本 |
| §3.2/3.3/3.5-3.8 | 預估 ~600 字 / 點 × 6 點 = ~3600 字 | ⏳ opus_worker M2.2 |
| §4 已驗證核心邏輯 | ~300 字 + 表 | ✅ Lead 寫完 |
| §5 對比 | ~400 字 + 表 | ✅ Lead 寫完 |
| §6 v3.0 預告 | ~300 字 + 表 | ✅ Lead 寫完 |
| §7 結語 + 致謝 | ~200 字 + User 補 | 🔧 Lead 半完 |
| 附錄 A/B | ~200 字 | ✅ Lead 寫完 |
| **總計** | **~7,000-8,000 字（M2.2 補完後）** | M2.2 待跟進 |

### 後續行動

- opus_worker M2.2 onboarding prompt（next Lead 動作、含 M1 截圖嚮導併入）
- M2.3 Lead 審稿（opus_worker 完工後）
- §7 致謝部分 User 補（指導老師 / 助教關係）

---

## [2026-06-04 23:30] 事件 #3 [opus_worker_2] [Phase 2] — M1 截圖嚮導 + M2.2 §3 六點 highlights + §2 配置表 + §6 v3.0 章

### 背景

接 Lead M2.2 派工（onboarding + 期中拍板 commit 8de8b0b）：Phase A 帶 User 補 8 張 demo 截圖、Phase B 寫 §3 六點 highlights（§3.2/3.3/3.5/3.6/3.7/3.8）+ §2 配置表 + §6 v3.0 章。期間 User 多輪 input、Lead 拍板 A1(a)/B1/C1/C2/D1/D2/E1，全納入。

### 目標

1. **Phase A**：嚮導 8 張截圖（demo_08/09/12/17/18/19/20/21）入 `reports/screenshots/E10/`
2. **Phase B**：6 點 highlights 四段式（問題/觀察/對策/驗證 + 對應 footer）、引述對齊真實事件 #、§2 加 live↔terraform 雙錨配置表、§6 升 v3.0 設計章（5 項）、§3.1/§3.7 voice 改 confident concept design、§4.1 數據 reconcile
3. 紅線：不編造（全數字 live 撈 / 事件可查）、不過度行銷、本機輸出誠實標未實作

### 處理步驟

1. **Phase A**：User 用「螢幕擷取畫面」批次截、我讀圖辨識 14 張 → 對到 8 目標 → 複製改名入 E10/（原檔留 Screenshots）。demo_08=processing elapsed 94s、demo_09=7066/May（caption 註）、demo_12/17/18/19/20/21=AWS Console 配置頁
2. **AWS live 驗證（D2、read-only）**：載 .env → `sts`(654485222392) → `lambda/sqs/athena/glue get-*` + `s3 ls` + Athena `COUNT`/`GROUP BY` → 全配置與 terraform **雙錨 MATCH**、§4 數據 live 復核
3. **Phase B 寫 6 點 + §2 + §6**（見結果表）
4. **D1 引述號自修**：§3.2 SQS = 事件 **#9**（非骨架誤寫 #6、#6 實為 M2 Parquet）；§3.3 F-H4 = 事件 **#1**（gate 批 A 設計）+ #11 + #14（非 #3）；附錄 B 同步修 + §4 列（F-H5 deploy 是 #14 非「實驗紀錄10 #1」）
5. **§4.1 數據 reconcile**：讀 demo_11 確認 = 903.5 KB（DOUBLE、F-H5 修前）、live = 76,673 B（INT32、修後）→ 確認 F-H5 連帶縮檔 12×、寫成 §3.4 附帶效益

### 結果與數據

**Phase A**：8/8 截圖入 E10（demo_08 48KB / 09 57KB / 12 68KB / 17 85KB / 18 55KB / 19 62KB / 20 90KB / 21 113KB）。

**AWS live ↔ terraform 雙錨（全 MATCH）**：

| 資源 | live 值 | terraform |
|---|---|---|
| Lambda | mem 2048 / timeout 900 / ephemeral 1024 / Image | lambda.tf ✅ |
| SQS clean-jobs | visibility 920 / DLQ maxReceive 2 / retention 1天 | sqs.tf ✅ |
| Athena tdcs-dl-wg | scan cap 10,485,760 / enforce true | athena.tf ✅ |
| Glue cleaned_v2_skeleton | 9 欄 + partition yyyymm | glue.tf ✅ |
| Athena COUNT(*) 202603 | **14,116** | — |
| GROUP BY vehicle_type | 5/31/32/41/42 = 111,926/980,484/338,957/10,767/87,699、總 1,529,833（scan 29.8KB / 778ms）| — |

**Phase B 寫作**（`期末書面報告.md`、22,404 字元）：

| 段 | 內容 | 自驗 |
|---|---|---|
| §2.1 | AWS 配置一覽表（8 資源、live↔terraform 雙錨）| ✅ |
| §3.2 | SQS broker（30s vs 847s、dual dispatch、事件 #9+#11）| ✅ |
| §3.3 | F-H4 SHA tag（事件 #1+#11+#14、tag a381a2d7a88a）| ✅ |
| §3.5 | Lambda>Glue（847s、M4.2 鎖板、CPU 報酬遞減取捨）| ✅ |
| §3.6 | E6 client-side（E1/E2 timeout 實證、+ streaming pull 概念）| ✅ |
| §3.7 | wizard 輸出（核心已驗證 + 本機下載誠實標待補、A1(a)）| ✅ |
| §3.8 | §0.3 四步妥協（事件 #12、14058→14116 +58 explained）| ✅ |
| §3.1 | voice 改 confident concept design（B1）| ✅ |
| §4.1 | 數據 reconcile 903.5KB→75KB（F-H5 縮檔）+ live 復核註 | ✅ |
| §6 | v3.0 設計章 5 項（BYO AWS / dedup / streaming / presigned / 彈性時間、B1 voice）| ✅ |
| 附錄 B | 引述號全修正（D1）| ✅ |

**自驗**：grep placeholder 殘留 = **0**；§3.2 引述 #9（無殘留 #6）；§6 五項關鍵字齊；報告 22,404 字元（超 5000-8000 字目標）。

### 後續行動

- **M2.3 Lead 審稿**：建議 Lead 看 (a) §3.7 本機輸出誠實寫法是否 OK；(b) §4.1 F-H5 縮檔 12× 是否要在 §3.4（Lead 段）也加一句；(c) §6 v3.0 把舊「純本地 DuckDB」改成「BYO AWS + IaC」主軸、是否保留 DuckDB portability 一句（我目前未提 DuckDB、聚焦 5 項）
- **回報 Lead 的 backlog（E1、非報告）**：`runPull` 清理只在成功時跑、Ctrl+C/出錯留孤兒 temp（本 session 幫 User 刪一個 3.4 GB）→ try/finally + SIGINT、連 streaming pull 一起做
- **S3 housekeeping**：bucket root 有 `202603/`、`202604/` 早期 root-level 上傳殘留（非設計 4 prefix）；demo_21 caption 已誠實註、Lead 可決定 demo 前清否
- M3 PDF（[USER_RUN] Pandoc）/ M4 brief / M5 PPT / M6 講稿待後續

---

## [2026-06-04 23:40] 事件 #4 [Lead] [Phase 2] — M2.3 審稿：拍板三問 + §3.4 §6 補強 + TOC 同步

### 背景

opus_worker_2 b3ff221 完工後三項拍板問 Lead：
- (a) §3.7 本機輸出誠實寫法 OK？
- (b) §4.1 F-H5 縮檔 12× 是否要加進 §3.4 Lead 段？
- (c) §6 改 BYO AWS + IaC 主軸、要不要補 backend portability 一句？

### 目標

逐項拍板 + 落實到報告檔、不再來回。守 §0.3 紅線（不編造、長期維護視角、邏輯正確性）。

### 處理步驟

1. **(a) APPROVED**：A1 框架本來就 (a)、opus_worker_2 寫法守紅線 #1 不編造、不改動
2. **(b) APPROVED + Lead 自編**：
   - §3.4「驗證」段後新增「附帶效益 — Parquet 檔案縮 12 倍」段
   - 含表（修前 903.5 KB DOUBLE / 修後 76,673 B INT32 / 縮減 ~12×）
   - 點出「明示優於推斷」不只正確性、也是效率紅利
   - 加重現指令 `aws s3 ls s3://112021024/cleaned_v2/yyyymm=202603/cleaned.parquet`
   - §3.4 工程方法論第 2 條補「同一份修正同時拿到『對』與『省』兩個結果」
   - §4.1 已有 F-H5 縮檔 12× summary、保留不動（兩段互相對照、不衝突）
3. **(c) APPROVED + Lead 自編**：
   - §6 結尾追第 6 項「Backend portability — 同一份 CLI、兩條 fork」
   - cloud-portable (v3.0a) + local-only (v3.0b) 並列表格
   - 點出 schema/清洗邏輯 → 跨 backend 可 swap（呼應 §3.4 + §3.8 sync copy 公理）
   - §6 結尾摘要句「最合適的地方執行」（原寫「留在雲端」、現含本地 fork 也成立）
4. **TOC 同步**：§3 目錄第 6 項從「Post-demo v3.0 規劃預告」更新為「v3.0 設計：自帶帳號、一次苦工、全民共享（含 cloud-portable + local-only 雙 fork）」、對齊 §6 實際標題

### 結果與數據

| 改動 | 位置 | 字數增 |
|---|---|---|
| F-H5 縮檔 12× 段 | §3.4 「驗證」後新增 | ~250 字 + 表 |
| 工程方法論第 2 條補強 | §3.4 收穫條列 | ~20 字 |
| Backend portability fork 段 | §6 結尾新增 | ~300 字 + 表 |
| §6 摘要句調整 | §6 末段 | 1 字「雲端」→「最合適的地方執行」 |
| TOC §6 標題同步 | §3 目錄 | 文字校正 |
| **報告總字數** | 從 22,404 字元 → 估 ~23,200 字元 | +800 字元 |

§3.4 + §4.1 互相對照、不衝突（§3.4 = 完整 narrative + 表 + 重現指令；§4.1 = summary 一句歸功 §3.4）。

### 後續行動

- M2.3 ✅ closed
- 進 M4 - M5 簡報製作：User 拍板派 opus_worker_2 用 D:\p\ppt工具\ppt-maker、內容 + 美術直接 User↔worker 討論（Lead 不寫死成品 brief、只指 source = 期末書面報告.md）
- M3 PDF / M6 講稿 / M7 錄影排程待 PPT 完後續派
- E1 backlog（runPull 孤兒 temp）+ wizard step 4 本機輸出未實作 已寫進 MASTER_PLAN §7 風險表 + PLAN_E10 後續行動段（commit 8de8b0b）

---

## [2026-06-06 01:15] 事件 #5 [Lead+User] [Phase 2] — Volume 補齊（4月匯入）+ R8/R15/R17 三項書面補項

### 背景

User 讀規則表 `workflow/reports/ref/期末作業規則.xlsx` 後發現現報告 narrative「工程方法論優先」與規則「資料量 + 數據分析 + Value + 費用 + 學習心得」配分不對齊。Lead gap analysis 顯示 5 項完全缺、2 項重大不符、估失分 -24~-49%。User 拍板「專題內容前置不變、根據工具補資料證明 narrative 對齊規則」。

### 目標

1. **R11 Volume**（10%）：從 4 週 6% → 8 週 8%，做法是把 ai_workspace 線下既有 4 月資料 S3-to-S3 匯入 mcp 主 bucket、Lambda 重清洗後 Athena 可查
2. **R8 GoogleMap**（1%）：截圖 + URL 進報告
3. **R15 AWS Bill**（5%）：Cost Explorer 截圖（既有素材庫 demo_22 已有）
4. **R17 學習心得**（5%）：4 組員各一段、初稿先寫進報告、組員後續最小修改

### 處理步驟

1. **AWS 雙錨確認**：
   - mcp bucket：`raw/yyyymm=202603/` 744 檔（4 匝道齊）+ `raw/yyyymm=202605/` 744 檔（cleaned 只 2 匝道、5月 demo run）
   - ai_workspace bucket（同 account 654485222392、bucket `112021024trafficdatacollectionsyste`）：`112021024/` 672 檔（3/29-4/25）
   - 結論：可走 S3-to-S3 同帳號內部 copy、無外網流量

2. **S3-to-S3 copy**（背景 task bya4xtzbt、~13 min）：
   - 指令：`aws s3 cp s3://112021024trafficdatacollectionsyste/112021024/ s3://112021024/raw/yyyymm=202604/ --recursive --exclude "*" --include "TDCS_M06A_202604*"`
   - dry-run 確認 600 檔（4/1-4/25、filter 排除 3/29-3/31 避錯分區）
   - 完成：600 objects / 3.69 GB 進 `raw/yyyymm=202604/` Hive partition

3. **Lambda clean 4月觸發**（job b5449bfd-e179-4918-b28f-85b4d9ef07f0）：
   - `POST /clean` body `{"year":2026,"month":4,"gantries":["01F2930N","01F2930S","01F3019N","01F3019S"]}`
   - API GW 立刻回 202 + job_id（SQS broker pattern、§3.2 narrative 再次實證）
   - 背景 task b6y21xw1d 輪詢、~13 min 跑完
   - 結果：scannedFiles=600、rowCount=11,384、parquetBytes=61,302（INT32、§3.4 fix 後）

4. **報告補項**：
   - 新增 §2.4「資料覆蓋範圍 — 8 週 / 3 個月」+ 3 個月 partition 表 + demo_24 截圖（User 拍 AWS Console「Calculate total size」一次顯示 3 partition / 2,088 objects / 12.0 GB）
   - §8 重構為 §8.1 學習心得（R17 4 組員初稿）+ §8.2 工程方法論收穫 + §8.3 致謝
   - TOC 同步加 §2.1-§2.4 + §8.1-§8.3 子標題
   - 既有 demo_22_cost_explorer.png（已在素材庫、Jan-Jun 6 月 cost 趨勢 + 6 service 分項 / Current $0.02 / Forecasted $0.19）已被 §7 引用、R15 完成
   - GoogleMap demo_23_googlemap_4gantries.png 已存（User 提供）、待 §1 或 §2 後續插入

### 結果與數據

| 指標 | before | after |
|---|---|---|
| 資料月份 | 1（3月） | **3（3+4+5月）** |
| 總 rows | 14,116 | **32,566** |
| 總 traffic | 1,529,833 | **3,499,466** |
| 總 raw size | 4.6 GB | **12.0 GB** |
| 總 raw 檔數 | 744 | **2,088** |
| R11 預估分數 | 6% (4週) | **8% (8週)** |
| R17 學習心得 | 0/4 | **4/4 初稿** |
| 報告章節數 | 8 主章 + 19 子節 | 8 主章 + **22 子節**（+2.4, +8.1-3） |

### 後續行動

- ⏳ User 截 §3 demo_07 啟動鏈剩餘缺項 + demo_08（清洗中、opus_worker_2 未交、User 自截）
- ⏳ R12 數據分析 4 query（**車種跨月**、**上下方向 N vs S**、**每週趨勢**、**每日 hourly pattern**）— Lead 跑 Athena、整合進報告 §4 或新 §4.3
- ⏳ R14 Value 章節 — Lead 從 R12 分析結果延伸出「對 OD/交通政策的價值」段
- ⏳ R16 會議紀錄 — User 補（先跳過、deadline 前處理）
- ⏳ 組員 review §8.1 學習心得初稿、各自修 1-2 句
- ⏳ M3 PDF 轉換（[USER_RUN] Pandoc xelatex）
- 5月 cleaned 仍只 2 匝道（南下）、現報告 narrative 寫成「demo run 選了南下 2 匝道」、不影響 R12 分析（用 3+4月 4 匝道全有資料做上下對比）

---

## [2026-06-06 02:00] 事件 #6 [Lead] [Phase 2] — R12 數據分析章節 + 視覺化（手動、仿期中、無 AI）

### 背景

User 拍板：「專題重點仍是系統 + 後段根據系統清洗結果做基本視覺化跟分析、不用 AI、Gemini API 分析也不放、做手動分析報告、參考期中 step2_visualization 結構就好」。對應規則 R12（15%）「數據分析（車種 × 每周 × 每月 + 上下交流道）」。GoogleMap URL 也需要進 §1（R8 完整、URL 已收）。

### 目標

1. 把 cleaned_v2 3 個月（202603 + 202604 + 202605）Parquet 下載到本機、用 `pandas` + `matplotlib` 仿期中 `step2_visualization/viz_4gantries.py` 結構生成基本視覺化
2. 補 §4.3「用清洗成果做基本視覺化與分析」章節、每組圖配 3-4 點手動觀察（無 AI）
3. §1 加 GoogleMap URL + 4 匝道表（R8 完整）
4. 不蓋過系統 narrative — §4.3 明示「不是專題重點、是清洗結果的可分析性實證」

### 處理步驟

1. **GoogleMap URL 進 §1**：在「同一套工具」句後加 4 匝道表 + demo_23 截圖 + URL 連結
2. **下載 3 個月 Parquet 到 `workflow/reports/charts_v2/_data/`**：S3 三次 cp、合計 178 KB
3. **寫 `workflow/reports/charts_v2/viz_3months.py`**（180 行）：
   - 仿期中 chart1（24H 多日疊加 + 紅日均線）+ chart2（車種 × 星期 grouped bar）
   - 新增跨月 3 圖：`_overview_daily_total`（4 匝道日總時序）+ `_overview_monthly_trend`（月總 bar）+ `_overview_vehicletype_by_month`（車種跨月、log scale）
   - 中文字型 Microsoft JhengHei + PYTHONIOENCODING=utf-8 解 cp950 編碼 bug
4. **跑出 11 張 PNG**（4 匝道 × 2 + 3 overview）
5. **§4.3 撰寫**：5 段（4.3.1 24H 通勤 / 4.3.2 車種 × 星期 / 4.3.3 跨匝道日總 / 4.3.4 跨月總車流 / 4.3.5 車種跨月）+ 小結
   - 每段 3-4 點觀察、手動撰寫、引清洗實際數值
   - 5月 narrative 強化「v2 上線首發 demo run、南下 2 匝道是設計選擇非 bug」
6. **TOC 同步**：§4 展開 §4.1-§4.3 子節

### 結果與數據

| 指標 | 內容 |
|---|---|
| 視覺化檔數 | 11（chart1 ×4 + chart2 ×4 + overview ×3）|
| 視覺化工具 | pandas + matplotlib（無 AI）|
| 視覺化資料 | 32,566 rows / 3 個月 / 178 KB Parquet（已 INT32）|
| §4.3 字數 | ~3,500 字（含 5 段觀察 + 小結）|
| R12 預估分數 | 15%（之前 0、現在落地）|
| §1 GoogleMap | URL + 4 匝道表 + demo_23 截圖（R8 完整 1%）|

**關鍵觀察**（reportable narrative）：
- 雙峰通勤型態（早 07-08 + 晚 17-18）跨 4 匝道一致
- 南下方向車流跨 3 月都高於北上
- 小客車佔絕對主導（其他車種 5-10 倍）、Bus 跨月最少（與此路段無客運常識一致）
- 週五最高週二最低（連假預備行為）
- 4 月車流比 3 月低 20-25% 主因是 cleaned 範圍少 6 天（4/1-4/25 vs 3/1-3/31）

### 後續行動

- ⏳ R14 Value（10%）— 接 §4.3 跨月 / 上下方向 / 車種數據、延伸寫「對 OD / 交通政策的潛在價值」段
- ⏳ §1 動機與目的對焦 R9（1%）— 把 §1 改成「如何用 AWS 雲端計算平台處理大數據」角度
- ⏳ §3.5 撞 R11/R13 字面救（5% 風險）— Lambda > Glue 改 voice
- ⏳ R16 會議紀錄 — User 補
- ⏳ 組員 review §8.1 學習心得初稿
- ⏳ M3 PDF 轉換 — Pandoc xelatex
- ⏳ 5月 cleaned 重 clean 4 匝道（可選、demo_09 截圖鎖在 7066/2 匝道、若重 clean 數字會變）— 留 demo 後決定

---

## [2026-06-06 02:45] 事件 #7 [Lead] [Phase 2] — 4 項規則補強：§1.0 動機 + §3.5 撞線救 + §4.4 Value + §5.1 services 表

### 背景

User 接受 §4.3 視覺化 + §9 會議紀錄後拍板「其他的就補一下吧、你帶我」。Lead 一次性推 4 項剩餘規則對齊：R9 動機（1%）+ §3.5 救撞線（-2~5% 風險）+ R14 Value（10%）+ R13 services（part of 15%）。

### 目標

1. **R9**：§1 新增 §1.0「動機與目的」首段、明示「如何用 AWS 雲端計算平台處理大數據」對焦規則字面
2. **§3.5 撞線救**：原 voice「Lambda > Glue ETL」、直接撞 R11/R13 字面、改寫成「Lambda Container + Glue Data Catalog 混合架構」、強調**有用** Glue Data Catalog（事實）、把 Spark Glue ETL 描述為「不同 sweet spot 的選擇」而非「不用」
3. **R14 Value 章**：§4.4 新章、接 §4.3 數據談潛在應用價值、含 4 子節（交通主管 / 學術研究 / 長期維護 / 誠實邊界）、不誇大、配實證數據
4. **R13 services 表**：§5.1 新增「AWS services 使用對照」表、規則表列 10 個 services 逐項回應、含使用 ✅ 與「不用 + 為什麼」🟡 兩類、附工程意涵段

### 處理步驟

1. §1 改成 §1.0 動機與目的 + §1.1 系統定位（原內容降到 §1.1）：
   - §1.0 開頭：「TDCS 每月 ~22 GiB / 744 檔、單機處理不來、必須借助雲端計算平台」
   - 點名 7 個 AWS services 在系統中的角色（S3 + Lambda + Glue Catalog + Athena + SQS + API GW + Terraform IaC）
   - 對齊 R9 評分字面「如何利用 AWS 雲端計算平台處理大數據資料」

2. §3.5 voice rewrite：
   - 標題：「Lambda > Glue ETL trade-off」→「Lambda Container + Glue Data Catalog 混合架構」
   - 開頭框架：「兩條成熟路徑、不同 sweet spot」（非對立）
   - 對策段：強調「兩者各司其職、不是二選一」+ 列出 Glue Data Catalog 實際用途（partition discovery、Athena 查詢入口）
   - 驗證段：Lambda + Glue Catalog 兩者協同達成完整鏈條
   - 不再主動講「不用 Spark」、改 framing「Spark Glue ETL 留給未來跨資料源 join 場景」

3. §4.4 Value 章（接 §4.3 後、§5 前）：
   - §4.4.1 對交通主管的價值（3 個決策場景對應表）
   - §4.4.2 對學術研究 / 民間第三方的價值（4 個研究主題對應表）
   - §4.4.3 對長期維護與社群的價值（3 點累積效應）
   - §4.4.4 價值量化的誠實邊界（明示「我們承諾的不是商業分析、而是把工程苦工降到接近零」）

4. §5.1 AWS services 對照表（接 §5 對比後、§6 前）：
   - 11 列表格（7 ✅ + 4 🟡 ETL/Cloud9/Step Functions/RDS-RedShift-EMR-Pipeline）
   - 每列附「用途 / 為什麼不用」說明
   - 工程意涵段：「未使用的 services 不是『沒能力』、是工程判斷選擇貼近 workload 的工具」+ 對比期中（0 services）

5. TOC §1 / §4 / §5 子節展開

### 結果與數據

| 補強項 | 規則 | 預估救分 | 字數增 |
|---|---|---|---|
| §1.0 動機與目的 | R9 | +1% | ~400 字 |
| §3.5 voice rewrite | R11/R13 救 | +2~5%（避撞線扣分）| ~300 字 |
| §4.4 Value 章（4 子節） | R14 | +10% | ~1,500 字 |
| §5.1 services 表 | R13 強化 | 強化 15% confidence | ~700 字 + 表 |
| **報告字數** | 從 ~57k → **63k 字元** | 估完整 ~15-17 頁 | **+6k 字元** |

**規則 R1-R17 對應狀態（更新）**：

| 規則 | 分數 | before | after |
|---|---|---|---|
| R7 封面 | 1% | ✅ | ✅ |
| R8 GoogleMap | 1% | ✅ | ✅ |
| R9 動機目的 | 1% | 🟡 | **✅** |
| R10 POC | 5% | ✅ | ✅ |
| R11 Volume | 10% | ✅ 8% | ✅ 8% |
| R12 數據分析 | 15% | ✅ | ✅ |
| R13 期中比較 + services | 15% | 🟡 | **✅** 含 §5.1 services 表 |
| R14 Value | 10% | ❌ | **✅** §4.4 新章 |
| R15 AWS Bill | 5% | ✅ | ✅ |
| R16 會議紀錄 | 3% | 🟡 1 張 | 🟡 1 張（User 補第 2 張） |
| R17 學習心得 | 5% | ✅ | ✅（組員 OK）|
| **R1-R17 預估** | **71%** | **~58%** | **~71%**（含 R16 partial）|

### 後續行動

- ⏳ User 補第 2 張會議照（明日）+ §9.2 紀錄
- ⏳ User 補組員貢獻度 %（封面表現有 0% / 0% / 0% / 0%）
- ⏳ M3 PDF 轉換 [USER_RUN] Pandoc xelatex
- ⏳ 5月 cleaned 重 clean 決定（demo 後）
- ✅ R14 Value 章誠實邊界段守住 §0.3 紅線（不編造商業分析）
- ✅ §3.5 voice rewrite 守住「不講不用 Glue」、實際 Glue Data Catalog 有用是真話、未編造

---

## [2026-06-04 19:25] 事件 #5 [opus_worker_2] [Phase 2] — M3 報告 7 點 refocus 改版 + 全截圖嵌入 + PDF 重生成

### 背景

User 直接下 7 點改版指示（非 Lead 派工、User↔worker 直接溝通），核心是把書面報告從「workflow 內部視角」轉成「對外功能介紹 + 已驗證成果」，並補齊視覺證據。先前版本程式碼用真實 TS/bash 區塊、配圖只用 `[配圖：x.png]` 文字註記、且夾帶內部代號（F-H*、PLAN_E*、mcp_workspace、資料夾結構）。

### 目標

落實 User 7 點：(1) 封面移除課程行；(2) 分工改表格、貢獻度全 0%；(3) §1 去本機/mcp_workspace、核心價值上移 + AWS 角色；(4) §3.1 改「未來有 TDCS 下載需求者」不寫全班；(5) **程式全改 pseudocode**；(6) 去資料夾格式 + workflow 內部資訊、聚焦功能與成果；(7) **圖片不省、連 TUI 都嵌入**。封面含學號保留本地不 push。

### 處理步驟

1. **pseudocode 化**（5 處）：§3.2 dual dispatch、§3.3 git SHA tag、§3.4 修前/修後 schema（2 塊）、§6 terraform apply——全部 TS/bash 真碼 → 中文虛擬碼描述
2. **去內部代號**：標題與全文 F-H4/F-H5 → 「git SHA tag 版控」「Schema 型別漂移」「型別修正」；殘留 PLAN_E8/E11、`backfill_s3_2026.py`、F-H1/F-H3 一併改描述性說法；TOC 同步
3. **截圖全嵌入**：所有 `[配圖]` 文字註記 → 真 `![](screenshots/E10/*.png)`；新增 §2.2「CLI wizard 操作畫面（TUI 五步）」嵌 demo_02/03/04/06/07/09/10、§2.3「AWS console 配置實證」嵌 demo_12/19/20/21；§3.x bug/console、§4.1、§7 各就位
4. **§6 「全班共用」** → 「共用同一個 bucket 與憑證、無法安全地開放給多人或長期使用」
5. **PDF 重生成**：Pandoc + xelatex，`--resource-path` 指向 reports/、所有相對圖路徑解析成功

### 結果與數據

| 指標 | 數值 |
|---|---|
| pseudocode 轉換 | 5 個程式碼區塊（TS×3 / bash×1 / terraform×1）|
| 嵌入圖片 | 22 張（架構圖 1 + E10 截圖 21，含 TUI demo_02–09）|
| 內部代號殘留 | 0（F-H / PLAN_E / mcp_workspace / 全班共用 / workflow 路徑 全清）|
| PDF | 2.32 MB（前 839 KB）、**0 缺字警告**、exit 0 |
| 圖路徑驗證 | 22/22 全部存在 |

### 後續行動

- M3 報告本體 ✅；封面（姓名+學號）僅本地、不 push GitHub
- §8 結語致謝待 User 補（指導老師/助教關係）
- 待 User 給簡報製作指示（M4–M5 PPT）

---

## [2026-06-05 01:50] 事件 #6 [opus_worker_2] [Phase 2] — M5 期末簡報製作（ppt-master 全流程 14 頁 + 講稿）

### 背景

書面報告定稿後 User 拍板進簡報製作，給定美術風格（自由設計 + 顧問風 + 深色 #325969 palette）、規模（~14 頁 / 10–12 分）、課程資訊（大數據資料處理 3A、授課教授 王經篤）。用 D:\p\ppt工具\ppt-maker 的 ppt-master pipeline，內容源 = 期末書面報告.md。

### 目標

走完 ppt-master 七步（init → 跳過模板 → Strategist 八項確認 → Executor 出 14 SVG + 講稿 → 後處理 → 匯出 PPTX），產出可交付的深色顧問風簡報，圖片不省（含 TUI + AWS console）。

### 處理步驟

1. **Strategist**：8 項確認（C Top Consulting、深色 #325969、微軟正黑體、AWS 官方圖標 + 內建庫、既有截圖無 AI 生成）→ 出 design_spec.md（13 章 + 14 頁 outline + SCQA）
2. **素材**：報告 .md + 22 張圖（架構圖 + 21 截圖）+ 8 AWS 圖標複製進專案 images/，analyze_images 掃描定 ratio
3. **Executor**：手刻 14 張 SVG（封面/定位/期中vs期末/架構滿版/wizard TUI 五步/AWS console gallery/4 大重點/驗證/對比表/費用/結語）+ notes/total.md 逐頁口語講稿
4. **後處理**：total_md_split（14/14 對應）→ finalize_svg（20 圖內嵌 + 圖標 + 55 圓角轉 path）→ svg_to_pptx -s final（native + svg 兩版）
5. **編碼坑修復**：工具內部 open() 走 Windows cp950 → 用 PYTHONUTF8=1 強制 UTF-8 模式解決
6. **QA**：svg_quality_checker 14/14 PASS 0 警告；Edge headless 渲 6 張代表頁實際目視（封面/07/10/12/13）版面乾淨無溢出

### 結果與數據

| 指標 | 數值 |
|---|---|
| 簡報頁數 | 14（含 TUI demo_02–09 + AWS console demo_12/17–21）|
| SVG 品質檢查 | 14/14 PASS、0 警告 0 錯誤 |
| 講稿 | 14 頁逐頁嵌入 PPTX notes、~11 分鐘 |
| 交付檔 | `期末/期末簡報.pptx`（1.73 MB native）+ `期末簡報_svg高保真.pptx`（1.77 MB）|
| 專題 | D:\p\ppt工具\ppt-maker\projects\tdcs_final_ppt169_20260604_193504 |

### 後續行動

- 期末三件套進度：書面 PDF ✅ + 簡報 PPTX ✅；YouTube（M7）講稿可複用 notes/total.md
- 封面含成員姓名 + 課程/教授 → 比照書面報告封面，**僅本地、不 push 教學 repo**
- 待 User 目視 PPTX 後決定微調（配色/頁序/文字）
- §8 書面結語致謝仍待補
